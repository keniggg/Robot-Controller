# 2026-09-11 控制权切换与自动运动确认恢复

操作者要求：勾选上位机关节直控时，滑条拥有最高优先级，机械臂不得自主
移动；取消勾选后才允许自主运动，滑条实时显示各关节实测角度。本次沿用
不自动发送扭矩失能命令的约束。

## 控制权

GUI通过锁存主题和同名持久参数`/gui/joint_direct_mode`发布选择，各关节
控制面板同步此选择，重启GUI继承参数。勾选后驱动只接受gui_direct和
gui_direct_sync来源，保护不再随0.25秒手势窗口结束。切换时串行清除旧
目标插值、端点补偿归属和未完成的运动确认，保留实测位置保持与已存在
的有效确认。取消勾选后拒绝旧滑条消息，控制器必须先同步到3mrad以内。

任务启动和每次绑定动作均检查直控模式；gateway拒绝自动位姿、关节、
夹爪、jog及确认恢复入口。勾选时取消当前MoveIt轨迹，驱动同时阻止非
滑条命令。此取消不停止控制器、不发送torque_off。GUI复选框自身不再
调用暂停/启动控制器服务。

自动模式的滑条变为只读，每次joint_states按关节名称更新数值，阻断Qt
valueChanged信号，避免显示刷新变成关节指令。手动模式保留操作目标，
实时反馈不会把正在调整的滑条拉回去。

## 自动确认恢复

原start_cb只接受CONFIRMED，而恢复只接在GUI滑条上，造成已对准后仍
等待手动运动的闭环。新增auto_confirm_actuation_from_observation，仅
对新鲜PENDING:POSITIVE_ENABLE_REQUESTED或COMMAND_SYNCHRONIZED生效。
DISABLED、过热、响应超时、缺失/过期状态继续拒绝。手动模式永不恢复。

start_cb先验证并冻结本轮FAR_FIELD_OBSERVATION_PLAN，再经原绑定动作
互斥入口调用新gateway服务。gateway同步实测位置、严格规划观察位姿，
仅取原碰撞检查轨迹开头每关节最多0.025rad的片段；裁切点位于原关节
路径边上，拒绝反向片段、非有限数据、过短轨迹及起点偏差超过0.003rad。
重新定时必须保持同一条位置路径，按原硬件速度限制执行；消费整个缓存，
不保留未执行的后半段。完成后必须读到真实CONFIRMED，任务才继续原
观察/近场/接触流程。无响应不重试、不放宽确认，不执行完整接触路径。

驱动原来只比较每次下发目标与最近反馈，在缓慢且跟随良好的轨迹上永远
达不到0.02rad命令变化门槛。现对短时间连续命令累计位移，仍要求真实
同方向编码器响应；间断、过长窗口、先于指令的反馈运动、归属改变和
反馈跳变都会丢弃该基线。没有由静止反馈或成功写torque_on伪造确认。

## 软件验证

GUI/任务/gateway/MoveIt/执行runner/串口共11组Python回归373项通过，
7条警告为已有rospy.warn弃用。覆盖未勾选时实时刷新且不发命令、勾选时
保留滑条目标、所有自动入口拒绝、接管取消轨迹、片段边界及错误起点，
还有绑定先于恢复、恢复失败不继续。60项C++回归通过，覆盖源归属、连续
响应确认和归属切换清除未完成确认；最新驱动编译成功。
部署及实机结果另行追加；这些软件测试不能作为已抓取成功的证据。

## 01:11部署及首次自动恢复结果

9bdc04c部署后，驱动由alicia-driver-control-20260911.service管理，任务
由alicia-task-control-20260911.service管理，GUI重启原有用户服务。
旧完整ROS的bringup.launch对motion_gateway设置respawn=true，退出旧
gateway后它已自动加载新代码，因此额外启动的gateway用户服务被同名
新节点替代而退出。最终有效gateway PID48406归完整ROS服务管理，含
新确认服务和模式订阅；不再启动额外同名实例。直控锁存值实测为false。

新计划23022da78e07f93b27d34069经完整绑定审计后调用/grasp/start。
01:12:56进入确认片段，但随后因ACTUATION_PREFIX_START_CHANGED拒绝
执行规划轨迹。同步阶段已产生小幅真实关节变化：Joint2约0.43718至
0.42491rad，Joint3约-0.16107至-0.17948rad；没有闭爪、抬升或成功持物。
根目录`.ros_log/grasp_attempt_20260911_control_recovery/`保留全部记录。

根因是控制器同步后又向驱动发布一次实时编码器位置，把伺服保持目标与
实测位置之间的固定偏差重复叠加，规划过程中起点继续移动。移除第二次
发布，并在控制器同步后要求实测角度连续0.25秒保持在一SDK量化步以内，
才从新位置规划。起点3mrad约束保留，失败诊断补充计划/实测向量。
连续响应确认也分别累计命令和编码器的位移，消除固定偏差的影响，仍
拒绝先于命令的运动、断流和无响应。针对gateway/规划/控制权/恢复的
108项Python回归通过。

## 01:19部署与真实自动运动确认

9bb73e6驱动编译成功，61项C++回归通过（新增固定伺服偏差下的连续响应
用例）。重启原driver用户服务并让完整ROS自动重生gateway加载新代码。
未发送失能命令。GUI截图与ROS锁存主题均显示直控未勾选，实测滑条值
跟随关节反馈；ROS、GUI及后台rosbag保持运行。

本轮计划0bda6c8eac359af16d9dd669完成绑定审计，确认片段实际执行后
驱动报告CONFIRMED:MEASURED_DIRECTIONAL_RESPONSE，motion_enabled=true。
随后34.392秒观察轨迹于01:22:04到达，进入近场预览。这证明此前“等手动
运动才能确认”的死锁已经实机打通，但并不代表抓取成功：本轮没有闭爪、
抬升或持物，近场以NEAR_FIELD_NO_HARD_SAFE_CANDIDATE结束。

数据位于根目录`.ros_log/grasp_attempt_20260911_settled_prefix/`，包含
runner.log、feedback.jsonl、每2秒RGB、GUI截图及绑定审计。request1443
配准成功，重叠率0.891、RMSE0.436毫米；四个通过开口范围筛选的方向
双侧实测接触高度仅1.383、1.136、1.272、1.320毫米，低于本轮2毫米要求。
其他方向包含开口范围超限。tabletop主失败为GRIPPER_CONTACT_PATCH_MISS，
因此之前只接受BILATERAL_SURFACE_EVIDENCE_MISSING的侧移补拍没有触发。

## 接触观测不足的补拍分类

将上述薄接触带接入原有一次侧移补拍：仅在CONTACT_EXECUTION_PLAN、
接触门禁未延后、至少一个通过开口范围筛选的接触带来自融合实测双侧表面，
且高度严格小于本轮原有接触要求时触发；仍要求同目标、同快照的成功配准。
空/畸形证据、足够高但CAD接触失败、碰撞、配准失败和过期快照继续拒绝。
不降低接触阈值，不修改候选放行；沿用一次补拍、原始截止时间、严格规划、
原18–22厘米观察距离及原碰撞/支持面条件。

remote streaming与完整任务序列回归536项通过，7条已有rospy.warn弃用警告。
新增14项涵盖本轮四条实测薄接触带、配准/时间戳失败、其他物理失败、
非接触阶段、非法边界和等于接触要求时不触发补拍。

## 混合方向与观察距离边界

b5f5848加载后执行计划995c129cf475b8a87daca34d。任务识别当前已经处于
观察区域，保留当前位姿，无重复观察运动。request50出现两条略高于2毫米
但后续CAD门禁未通过的接触带，以及三条1.828、1.806、1.020毫米薄带。
本轮因此仍结束于无可执行近场候选，没有闭爪；数据位于根目录
`.ros_log/grasp_attempt_20260911_thin_surface/`。补拍条件从“全部薄带”
纠正为“存在独立可补观测的薄带”，其他方向的物理失败仍不放行。

该轮冻结快照实测相机距离0.21894米，也暴露旧纯横移公式无法在最大
0.22米、预留5毫米端点误差条件下产生侧移。近场恢复改为围绕冻结目标的
两个对称观察位姿：由侧移量与目标相机距离求径向分量，保持相机朝向目标，
严格保留实测手眼安装变换。起点仍须处于原18–22厘米范围，新位置保持
5毫米距离余量，后续物理包络、严格MoveIt、单次预算和原截止时间不变。
原最终细化的向外退让流程未使用该可选观察距离参数。

上述两组回归537项通过，覆盖混合接触方向、21.9厘米边界下保距侧移、
起点超出观察距离时不执行，以及截止时间不足时不执行。

## 01:37近场候选与旧中心代理冲突

7474760加载后，计划630a49abd8213217853f40b3保留当前观察位置。此次
近场直接生成并绑定9c1bace0b5434413f8a128a9，无需侧移；随后却被旧
NEAR_FIELD_CENTER_ANCHOR_MISSING阻止，仍未闭爪。根目录
`.ros_log/grasp_attempt_20260911_surface_orbit/`保留全部证据。

远场点云中心(-0.116940,-0.381668,0.078867)，近场融合中心
(-0.116393,-0.380939,0.078926)，相差约0.91毫米。近场3D配准1024个
内点、0.863重叠率、0.560毫米RMSE、0.125毫米平移修正。但任务端把
检测框表面点减去半高得到的中心代理(-0.1155,-0.3786,0.0745)与真实
融合OBB中心比较，得出3.1毫米残差；这两个量并不具有相同几何定义。
远端已经改用注册点云，保留的reference_center不再用于强行移动点云。

接触执行计划的连续性现复用已存在的三维配准与支撑面一致性验证，要求
同目标、真实VALID_3D内点/视图数量、重叠率、RMSE、位姿修正及支撑面
全部通过原共享阈值。缺失或不足的3D证据直接失败，不回退到中心代理。
旧非接触计划仍保留平面中心校验；诊断文字不再把超限描述为within。
任务与GUI控制权回归215项通过，新增用例覆盖真实配准通过、缺失/弱证据、
目标不一致和支撑面改变。

## 01:43最终运行记录与未完成项

30a6375任务节点加载后启动78381d6df9e6dc7ba62bbf10，保留观察位姿。
request122正确报告NEAR_FIELD_SURFACE_VIEW_REQUIRED，一次补拍恢复已
进入严格规划。负向补拍可达，Joint6需约1.94rad，硬件速度限制下轨迹
下限约96.99秒；正向不可达。剩余近场预算不足，任务在运动前报告
NEAR_FIELD_DIRECT_TIMEOUT，未执行该补拍，未闭爪、抬升或成功持物。
证据位于根目录`.ros_log/grasp_attempt_20260911_registered_continuity/`。

追加只读IK诊断，以当前实测状态直接作为种子，负向补拍仍需1.9418rad，
正向无解；因此不能把该大幅关节调整简单归因于随机规划种子。保持相机
方向的4/2/1厘米候选也分别需要最大约1.79/1.42/0.87rad（另一侧2/1厘米
约0.98/0.73rad）。这些仅调用IK，未规划执行或改变运行配置。后续抓取
仍需处理观察运动的运动学代价和阶段时间安排，不能宣称高成功率抓取已完成。

本次用户明确要求的控制权和反馈功能已部署，复选框锁存值仍为false，
motion_enabled=true、actuation_status=CONFIRMED。ROS、GUI及后台rosbag
继续运行。所有生产代码修改均已提交至codex/protocol-v3-upgrade；未向
驱动发送失能命令。
