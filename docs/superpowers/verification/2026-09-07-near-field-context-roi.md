# 2026-09-07 近场 GraspNet 输入范围调整

## 实测失败

18:42:54 启动计划 `f32118ee07a8b9cd540fc215`。机械臂于 18:44:41
到达观察位，实测相机距目标 0.1933 m。近场阶段成功建立 309 点的
到达视角参考表面，但后续请求在 GraspNet 输入构建时返回
`TARGET_FRACTION_INSUFFICIENT`：目标占比 0.145842、0.143860，低于
既有 0.15 门限。18:45:42 任务以 `NEAR_FIELD_DIRECT_TIMEOUT` 结束，
尚未执行接近、闭爪或抬升。

## 同源 RGB-D 复现

只读订阅现有相机、目标、joint states 和 TF；每组使用三个时间戳匹配且
通过既有稳定性条件的帧。未发布控制命令。三组数据的源时间戳、掩码、
深度、相机内参和支撑平面保存在工作区根目录
`.ros_log/grasp_attempt_20260907_1842/`。

| 组 | 目标深度点数 | 原比例 0.30 / 40 px | 比例 0.25 / 33 px | 调整后支撑点数 |
| --- | ---: | ---: | ---: | ---: |
| 0 | 5286 | 0.144084，拒绝 | 0.170500，通过 | 25717 |
| 1 | 5319 | 0.144861，拒绝 | 0.171393，通过 | 25715 |
| 2 | 5322 | 0.144883，拒绝 | 0.171406，通过 | 25727 |

调整部署配置的 `graspnet_input_context_expand_ratio` 从 0.30 到 0.25。
该参数仅决定送往 GraspNet 的目标周围支撑面范围；输入仍使用
`context_roi`，目标深度、目标几何和后续碰撞几何的构造均不变。
回放逐像素断言所有目标深度均保留；15% 目标占比、200 支撑点、
6 mm 支撑面距离和实例一致性条件均保持原值。

回放三组原配置均失败、三组新配置均通过，退出码 0。
这证明输入占比故障得到复现并可用现有配置消除；不代表实物抓取成功。

输入构建与连续推理回归：`test_graspnet_input_context.py` 和
`test_remote_grasp6d_streaming.py` 共 **389 passed，27.85 s，exit 0**。
YAML 解析确认仅扩展比例变化，`git diff --check` 通过。
