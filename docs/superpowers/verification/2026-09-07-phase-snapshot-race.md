# 2026-09-07 继续抓取与阶段采集竞态

`dd31053` 部署后的19:41抓取 `e158b8988ef66b6eecdc0489` 到达观察位，
观察运动85.8秒，实测距目标0.1834m。近场配准373 inliers、488融合
点，实测最大修正0.0846mm；旧桌面净空和配准误拒已消失。31个
tabletop候选在后续单侧伸达检查被拒（25.23–27.96mm >25mm），
未闭爪或抬升。录制保存在根目录 `.ros_log/grasp_attempt_20260907_contract_repaired/`。

操作者要求继续后，21:36对当前观察位采集两组RGB-D。真实注册
185 inliers，实测最大修正3.16mm；离线得到6个通过完整几何门的
候选，开口37.67–40.20mm。数据与角度分析保存在
`.ros_log/grasp_attempt_20260907_contract_repaired_surface/`。

21:39新计划 `7da8440b991cb3dbf9e2342b` 复用已达观察位（距目标
0.1824m），没有重复绕腕运动。但21:40:02第一份近场结果失败。
日志显示它错误地使用了5帧远场采集窗口，包含阶段开始前的图像；
近场配置要求阶段开始后的3帧。原因是 `_poll_stream_snapshot`
在等待采集/融合期间允许阶段切换，提交时却给旧采集结果分配了
新一代请求。该旧请求抢占了单快照近场流程。

修复将采集开始时的generation传入 `submit_stream_snapshot`，在
与队列提交相同的锁内比较。跨阶段数据丢弃，下一次轮询正常采集
新阶段三帧。两项回归分别在采集和融合中途切换阶段，修复前均红，
修复后验证旧批次不入队、新批次正确提交且每阶段仍只提交一次。

验证：streaming中 poll/submit/phase相关29项通过，diff检查通过。
本次尚未证明实际持物成功；所有操作保持关节使能。
