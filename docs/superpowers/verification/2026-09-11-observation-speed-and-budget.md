# 2026-09-11 观察速度与补拍预算

操作者指出尚未真正抓取，第一阶段运动过慢，并先自行移动/重新对准。
本轮在操作者再次报告对准前不执行自动运动。读取到关节直控为true，
任务inactive；ROS、GUI和记录服务维持运行，不向驱动发送失能命令。

## 现有证据

上轮78381d6df9e6dc7ba62bbf10已触发一次补拍，原轨迹Joint6位移
约1.94rad，0.02rad/s下限速决定至少约97秒，超出剩余60秒阶段预算。
并未闭爪、抬升或成功持物。直接用实测关节角作为IK种子仍得到同等位移，
不能归因于随机起点。

驱动配置为15deg/s、软件速度上限0.30rad/s，规划全局限制0.08rad/s，
但第3/4/6关节额外限制为0.02rad/s，源于旧版反馈停顿记录。
修复驱动后的本次手动滑条数据见根目录`.ros_log/driver_control_20260911.log`：
1789116575–6589期间1秒左右的反馈差分，Joint3最高约0.087rad/s，
Joint4约0.227rad/s，Joint6连续区间约0.227–0.235rad/s。
这证明0.02不是电机绝对速度上限；手动点到点速度并不证明连续轨迹同样
速度下的跟随精度，自动观察仍须用实际期望/反馈与既有容差验证。

## 观察与接触的独立速度

增加三个明确的观察专用严格规划/执行服务，仍使用SetTargetPose协议。
gateway在现有可重入规划锁内使用独立MoveItPlanner和独立缓存：观察
配置最高0.08rad/s，保留原加速度缩放、路径逐点速度约束及执行反馈容差；
接触实例保留Joint3/4/6的0.02rad/s。观察速度不能超过原全局0.08上限，
非法值拒绝。调用结束或异常时恢复普通planner，直控接管仍取消正在使用
的实例轨迹；手动模式在构造观察planner前拒绝执行。

远场候选排序调用只读观察严格服务，以同一速度规则估算耗时。任务按
已冻结计划的FAR_FIELD_OBSERVATION_PLAN/CONTACT_EXECUTION_PLAN选择
服务，观察预抓取、观察修正和第一次补拍使用观察速度；近场绑定后回到
接触速度。没有冻结观察计划时不选择快速配置。运动确认片段使用观察
实例，原0.025rad片段边界与最短3秒定时仍有效。

## 完整阶段预算

近场入口一次分配90秒，涵盖初始注册快照、一次补拍运动及第二次快照。
中途不续期；补拍执行前除运动与0.9秒稳定时间外，额外预留20秒新推理。
选中方向重新规划后使用新返回的耗时，而非此前可能过时的预估。
补拍次数、身份/时间戳绑定、观察距离和物理几何门未修改。

## 回归

gateway、完整任务序列、remote streaming、GUI控制权、MoveIt反馈共
628项通过，7条已有rospy.warn弃用警告。覆盖观察/接触缓存隔离、异常
恢复、手动执行拦截、冻结阶段路由、远端只读服务选择、第二次推理预算、
重规划耗时变大时拒绝执行。任务/gateway单元测试隔离现场ROS参数，避免
操作者的实际复选框状态影响单元断言；测试替身已支持新观察服务。

这些结果证明软件回归通过，尚不代表提速后成功持物。部署和实机数据
另行追加。

## 02:07 实机执行与直控交接修复

操作者明确回复“已对准，直控已取消”，部署71e2db4后启动新任务。
证据目录为根目录`.ros_log/grasp_attempt_20260911_observation_speed/`。
新远场计划cd69b1000baf8f4c919a0c9b已通过绑定审核并进入MOVE_PREGRASP。
观察轨迹重定时12.193秒，峰值0.050rad/s；2.46秒后Joint2跟随误差
达到0.120rad，控制器返回PATH_TOLERANCE_VIOLATED。

这次的关键证据是控制器期望角变化，而六个实际关节角和驱动SDK目标
都没有变化。取消直控时设置control_mode_needs_sync_，但自动命令分支
从未填充局部feedback_joint_angles；交接比较尺寸时静默return，后续
自动命令因此全部被丢弃。不是本轮实测电机无法跟随0.050rad/s。

修复在交接判定处持data_mutex读取当前六轴编码器及其接受时间；要求
反馈新鲜、六轴齐全、数值有限且目标误差不超过原0.003rad。未满足时
输出明确同步等待日志，成功后输出自动控制交接日志。保持原模式互斥锁，
未放宽跟随容差，未发送失能命令。另将配置契约测试中的旧60秒断言更新
为已部署的90秒，并覆盖20秒推理预留。

驱动与C++测试二进制重新构建成功，62项C++测试通过；配置与MoveIt
执行配置21项Python测试通过。新增交接测试覆盖缺失/陈旧/非有限反馈、
未同步旧目标、六轴实测同步以及0.003rad边界两侧的输入。

## 02:14 实际观察成功，继续处理混合候选失败

e38b7be部署后的40aca051a8914ef7b8b2bc71完成实测运动确认和第一阶段
观察轨迹。轨迹重定时11.437秒，控制器返回成功；580个控制器状态样本
的逐轴最大绝对跟随误差为[0.011966,0.010777,0.018295,0.012735,
0.025091,0.007812]rad。稳定后的相机目标距离0.2019m，通过18–22cm
观察契约。证据目录`.ros_log/grasp_attempt_20260911_handoff_fixed/`。

随后请求127返回NEAR_FIELD_NO_HARD_SAFE_CANDIDATE，尚未闭爪。
注册重叠87.0%、RMSE0.498mm；14个已生成桌面候选沿物体较宽方向，
单侧需要至少26.10mm，超过25mm。其他较窄方向在生成器内因为双侧
表面观测不足而被丢弃，诊断未保留，最终“碰撞”总失败码掩盖了补拍
机会。相同请求的单帧保存点云只读重放（不是完整在线融合重放）保留了
15个可容纳但缺失表面的方向；61.2165度方向单侧需要19.957mm。

生成器现在单独输出surface_evidence_rejections，记录缺少双侧实测
表面且通过当前OBB单侧可达范围的方向；这些诊断不成为抓取候选。
近场分类器在相同身份、时间戳和成功注册条件下识别此诊断，允许已有
的一次补拍。没有放宽50mm夹爪、碰撞、接触高度或跟随限制。

桌面候选、夹爪几何和远端流处理共517项回归通过。新覆盖包括超宽
OBB不能获得补拍方向，以及其他方向碰撞、陈旧注册和非法可达范围
不会错误授权接触或补拍。
