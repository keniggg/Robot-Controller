# 2026-09-13 再次交权：反向运动误报修复与新抓取运行记录

## 本轮入口

操作者明确取消直控并再次交权，要求执行新一轮 6D 抓取、监督数据。
01:53:38–46 实时读到 GUI_DIRECT=false、task IDLE/inactive，目标约
0.312 m、置信度约0.90，accepted串口反馈持续更新；驱动却保持
`UNCONFIRMED:ENCODER_RESPONSE_LOST`。未绕过这个运动确认门启动接触。

该状态不是助手发送失能：`motion_enabled` 是逻辑“运动已确认”话题，
驱动仍保持此前 SDK 目标。未发 `/grasp/stop`、torque_off、归零或控制器
停止/卸载命令。温度不作为本轮主要排查方向，原保护没有更改。

## 新定位：确认后的保持/反向运动错误复用测量起点

故障第一次出现在1789288149.045818（01:29:09），并非本次取消直控时。
当时GUI在1789288133.987进入直控。正常关闭的 `telemetry_3.bag` 记录：

| 时刻，ROS秒 | Joint5已发SDK计数 | Joint5独立accepted计数 |
| --- | ---: | ---: |
| 1789288147.004 | 2092 | 随后继续上升至2084 |
| 1789288147.924 | 2085 | 2084 |
| 1789288148.004 | 2079 | 2084 |
| 1789288148.044 | 2071 | 1789288148.046为2082 |
| 1789288148.124 | 2058 | 随后2070、2058、2056 |
| 1789288149.046 | 2058保持 | 2056保持，却报ENCODER_RESPONSE_LOST |

反向实际从2084到2056，约28 count/2.46°，不是此段完全没有编码器响应。
源码 `ActuationConfirmation::note_streamed_target` 在已有CONFIRMED后，
前段动作仍在收敛而SDK保持不变时，没有同步更新下一次运动的实测起点。
反向新命令累计到0.02rad后，会相对于前段正向运动较早的编码器起点判断
方向；即使新的反向动作已发生，只要尚未越过这个旧起点，仍被判无响应。

新增基于真实计数的约化回归：旧源码失败；修复后通过。此证据确认这条
软件缺陷，但不证明此前所有无响应、Joint2死区、11.83mm或跨姿态误差
均由同一问题造成。

修复仅在CONFIRMED、尚无新SDK位移累计时，把测量基准更新到最新反馈；
一旦新位移开始累计，就冻结该基准。已激活probe仍保持原期限，不用
keepalive重置；PENDING、真实停滞/反向错误/非命令轴运动/保护门不放宽。
另补“前段运动不得冒充新命令响应”的静止、反向、其他轴反例。

## 验证、加载与记录

- C++ 93项全部通过（含新约化反向用例和新命令无响应反例）。
- Python驱动、端点、参考交接专项176项通过，8.24秒；diff检查通过。
- 诊断目录：根工作区 `.ros_log/grasp_rehandoff_20260913_Jew54l/`。
  `manual_reversal_source.bag` 是正常关闭原卷的保留副本，不读取active卷。
  `actuation_reversal_tests.xml`、重载前后参数、watch/runner日志及同源
  RGB-D关键帧bag在同目录。纯订阅录制/终端监测在02:02前启动。
- 02:03只替换driver和gateway；旧PID6794/6813退出，driver新PID33553，
  用户服务 `alicia-driver-reversal-20260913.service`；gateway由原launch
  的respawn机制重启为PID33395，不再另起第二个实例。
- 驱动新二进制SHA256
  `66550cc45cafb0e591d413d0544322ea2611f6cfc9e3c93003879b7d8999a2ad`。
  网关源码SHA256
  `bdf7dc845d95d3e17faf103a7e62246f1d6e6f3883f7df70662985d69b0fd358`。
- 驱动析构仅断开串口，没有失能钩子；新驱动沿原参数于1789290233.278
  请求一次正向使能，状态PENDING而非伪造CONFIRMED。GUI、相机、推理、
  task、硬件接口和控制器未重启。重载前后实测六关节未变，未发新SDK
  位置目标；watch中的旧SDK缓存会显示年龄增大，不能误认成新驱动目标。
- 最新有界端点实验服务已随gateway加载，但执行开关仍默认关闭，本轮
  未调用；不把上轮模拟补偿作为实机目标，也未自动插入生产抓取流程。

## 新抓取请求与实际结果：观察已运动，未抓取成功

02:04:32启动 `run_fresh_grasp6d_after_alignment.py`，本次最小源戳
1789290272117150545，候选等待300秒、原计划有效期120秒。只接受本次源
窗口中有精确审计绑定的新远场计划；启动受限观察前段重新验证实际响应。
不重用上轮计划、不改18–22cm、3mm净距或接触6mm/5°门限。

计划 `8335d11806e7ec4e9fca5e28`，源戳1789290290676292419，绑定摘要
`cc8fa1c3c4d91a90b99f45eedd7827ea6ba1ab97b79a6d987b208a8b96a27477`。
观察选择检查19/23个姿态，11个可达，选roll0°/tilt15°，名义硬件时间
下界7.731秒。审计与源时限通过后才调用/grasp/start。

| 时刻，PDT（ROS秒） | 实际事件 |
| --- | --- |
| 02:05:42.993（1789290342.993） | 受限确认前段action：2点、3.0秒 |
| 02:05:44.888（1789290344.888） | 新编码器响应，CONFIRMED:MEASURED_DIRECTIONAL_RESPONSE |
| 02:05:46.043（1789290346.043） | 前段action SUCCEEDED/error_code0 |
| 02:05:47.373（1789290347.373） | 主观察action：14点，最终含接续的定时长度12.615674秒 |
| 02:05:59.908（1789290359.908） | 主观察action SUCCEEDED/error_code0 |
| 02:06:00.922（1789290360.922） | 实测CAD净距失败OBSERVATION_SUPPORT_COLLISION，left_finger |
| 随后 | task FAILED/inactive，runner退出6，未进入近场、闭爪、抬升 |

本轮近场mappingproxy路径没有执行到，不能用本次运动完成宣称近场实机
验证通过。新运动确认在后续02:08被动窗口仍CONFIRMED，未再掉为本次
已定位的旧起点误报；这不是所有方向/姿态的实机回归验收。

## 本次未完成的直接原因及量化证据

请求TCP为 `[-.104949623,-.329316990,.088607547] m`；实际为
`[-.100328942,-.323604640,.078772802] m`。相对请求误差12.276149mm、
约1.97°，其中Z低了9.834745mm。独立同模型SDK—accepted误差
12.188265mm；不得与请求误差混写或沿用上轮11.833428mm。

accepted−SDK码差 `[+5,-9,-11,+2,-2,-4]`。Shapley沿总误差投影：
Joint2约4.460mm、Joint3约6.572mm，合计约90.5%；这不是可以直接发送的
补偿量，也不能据此断定机械损坏。原0.035rad关节容差可通过而TCP仍偏位。

使用冻结支撑面、相同模型及实测开口49.75mm，分别复算SDK目标和accepted：

| CAD盒 | SDK终点名义净距 | 实测关节FK净距 |
| --- | ---: | ---: |
| 左夹指 | 9.721157mm | 1.797297mm |
| 右夹指 | 13.951657mm | 5.729378mm |
| 掌部 | 57.124402mm | 50.154231mm |

原要求3mm。当前左指不是模型中的负净距，而是安全余量少1.202703mm。
错误名带COLLISION不等于已经证实实体碰桌；真实接触仍需要现场观测。
任务的实时CAD日志使用TF/accepted相差约9.7ms的新鲜样本；之后同一冻结
URDF用accepted直接FK复算得到相同净距，不能将本次1.797mm归为旧TF。

前段连续名义指令净距认证51.790mm；主观察连续名义指令认证下界
3.055281mm（采样最小9.737517mm）。**这些只认证commanded Link6/7/8，
不包含真实关节跟随误差。** SDK终点左指9.721mm看似合格，实测偏移却
损失约7.924mm净距。这是本轮暴露的生产规划缺口：应在运动前为跟随
误差预留余量，不能仅在运动后拒绝，也不能把3mm门降为1mm来算成功。

## 下一步与停止推进运动的边界

失败后没有回退、叠加端点校正或重放计划，没有失能。新端点校正实验
依赖新鲜场景/参考及有余量的自由空间，当前低净距姿态不能直接启用。
已有源计划已过期，也不能用“重试”让旧场景恢复运动权限。

已向操作者询问当前夹指与桌面是否仍有实体间隙。需要这个现场信息才
能决定后续姿态恢复，不沿用旧回合的“未接触”确认。软件后续工作应优先
把可验证的跟随误差余量纳入观察候选选择及最终连续路径校验，在满足
原观察距离/限位/碰撞约束的候选中选时间较短者；若无足够余量，则须在
有净距姿态完成小步响应/固定目标校正验证，而不是继续近桌盲积分。
这不改变6D主路线，也不是宣称已实现上述生产规划补全。

`rehandoff_and_attempt.bag` 已正常关闭，约163MiB、136组精确同源关键帧；
动作、TF、原始诊断、参考、任务终态及审计同存。`following_at_end.json`
和`runtime_execution_error.yaml`保存上述实测分解；全量原始rosout在该bag
及ROS run `f0e99d88-af48-11f1-8feb-3db53bbffd36`。02:08–02:09只读窗口
关节保持不变、task inactive、GUI false、运动确认CONFIRMED。主ROS、新
driver和持续遥测服务active；本轮临时watch/关键帧记录已正常结束，不冒充
助手回合结束后还在持续人工判断。

后续追记：操作者已明确本次前夹指仍有实体间隙、没有接触，之后取消
直控。生产候选和最终曲线的条件支撑跟随余量补全已离线验证并重载
gateway/remote；没有把现场非接触改写为模型满足3mm，也没有在本轮
发送新运动。具体范围、测试、部署和仍待实机验收项见
[前置余量修复记录](2026-09-13-prospective-observation-following.md)。
