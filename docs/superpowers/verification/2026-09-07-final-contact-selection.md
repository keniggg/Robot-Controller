# 2026-09-07 最终接触校核参与候选选择

21:44真实抓取计划 `8b4e89db172d2ad6e11b5f4a` 复用观察位，距目标
0.1868m。阶段7于21:44:58.312开始；近场请求45的源帧时间
1788842703.6463478，确认阶段采集竞态修复已生效。

21:45:12.422首个可达候选在配准后最终几何校核失败：双侧实测点
16/9，未满足要求。异常从预览构建直接逸出，成为 ACCEPT_FAILED；
候选选择已经提前结束，后续候选没有机会接受最终校核。
任务于21:45:58等待超时，未闭爪、未抬升。证据保存于根目录
`.ros_log/grasp_attempt_20260907_phase_repaired/`。

修复使 single_snapshot_direct 的有界排序选择对每个原始可达候选
继续构建并验证最终注册后的完整接触序列，然后才允许选中。
接触失败返回检查失败，继续排序列表；原始接触、碰撞、路径和
关节位移限制不变。选择使用最终严格MoveIt指标，发布时沿用精确
序列缓存并重建绑定预览。候选检查本身没有发布行为。

几何失败的原始错误码保存在 final_contact_rejection 和日志中；
MoveIt结果使用通用检查失败，不伪造IK/碰撞布尔证据。

验证：新增3项回归先失败，修复后通过，覆盖首项失败选次项、
原始不可达不继续构建、最终关节位移超限仍拒绝。
streaming/pipeline/新回归共486项通过（105.06秒）；新增流程接线
断言后定向4项通过。git diff --check通过。
这些是软件证据，尚不代表真实持物成功。
