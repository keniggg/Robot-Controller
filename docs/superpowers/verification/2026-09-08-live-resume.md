# 2026-09-08 实机恢复和观察距离数值边界

03:00按操作者请求恢复上位机。原完整ROS服务、驱动、相机、感知、
MoveIt和339befd规划节点仍在运行；单独启动gui.launch。
发送 `/demonstration=false`，驱动映射为正向torque_on；未发送失能。
03:01实测 motion_enabled=True、CONFIRMED:MEASURED_DIRECTIONAL_RESPONSE，
run_status=0。操作者移动后03:02确认对准。

后台 `alicia-feedback-recording-20260908.service` 持续只读录制关节、
使能、状态、感知与规划指标；128MB分卷、最多4卷。相关数据在根目录
`.ros_log/alignment_20260908_0300/`。

新鲜计划 c655a59c4aabff5700b93d38 通过规划器并提交执行槽，但运行器
错误拒绝 actual_camera_target_distance_m=0.22000000000000006；
配置上限为0.22。该ULP舍入来自刚体变换，未启动运动，随后运行器
等待授权超时。原始绑定audit已保存为 boundary_audit*.json。

修复运行器距离比较，使用1e-12m纯数值容差。配置区间仍严格为
18–22cm。回归覆盖两端ULP误差、两端实际超出1微米、NaN和运行器
推理生命周期：修复前2失败/4通过，修复后6通过。
尚未证明本轮真实持物成功。

## 已到观察位后的旧计算阻塞

修复边界后，03:09执行计划 `625ca7e70981993b67a3d72d`，观察运动
21.64秒、到位误差9.3mm/1.89度，实测相机距目标0.2081m。
03:09:59.495任务进入近场阶段8，03:10:06.962规划器完成阶段切换。
然而旧远场请求3289仍在稳定候选和观察位循环中运行，到03:11:08
才报告GENERATION_STALE。新近场请求3290在队列里等59.97秒；任务
已经03:10:59超时。没有闭爪或抬升。精确日志和远场audit保存在
根目录 `.ros_log/grasp_attempt_20260908_0309/`。

在稳定候选每个变体、观察距离/角度每个视图开始前验证请求代际，
并让StreamResultCancelled穿过候选局部异常处理，及时释放worker。
没有改变运动、接触门槛或超时。两项回归模拟循环内阶段切换，
确认不会再开始第二个旧变体/视图。旧行为测试均失败；修复后的
streaming/观察距离其余316项通过，修正新增测试的启用状态fixture
后新增2项通过；diff检查通过。

## 近场候选生成重复工作

2cb6497部署后计划 `7e2260edaa83da21a792209c` 复用20.79cm观察位，
近场阶段9于03:18:58开始，旧远场请求03:19:10退出，近场请求56
得到处理。但本地准备耗时28.94秒，结果年龄34.60秒，被有效期门
拒绝；03:19:58超时，无闭爪抬升。

只读重采样保存于根目录 `.ros_log/grasp_surface_profile_20260908/`，
415/408点，真实配准309 inliers、最大修正2.83mm。cProfile定位
接触边界二分在每次只用一个分支时生成全部6个姿态。增加可选
branch_key，仅计算指定夹爪分支/倾斜极性，保留同一CAD求解、
校核和源编号；默认完整生成不变。边界搜索的14步和搜索范围不变。

同一真实数据回放候选生成13.129秒降到6.958秒，整个输出JSON
逐值相等。该快照仍有32个单侧伸达等拒绝，优化不代表物理可抓。
新增11项覆盖6个分支和非法输入；tabletop/streaming共340项通过。

### 此前审批阻塞（03:42已恢复）

自动审批在Git提交处返回用量上限错误，明确禁止绕过；该提交未
执行，同一编排中后续规划节点重载、录制和抓取命令也未执行。
branch_key优化已经保存到工作树文件，尚未提交、尚未加载到运行
节点。当前已提交并部署版本仍为2cb6497（含f1c1255运行器修复）。
额度/审批恢复后先提交上述优化，再只重载规划节点；开启新录制，
运行tools/run_fresh_grasp6d_after_alignment.py，继续监督实际结果。

03:42操作者要求继续后，审批恢复，优化提交为2c58d75并重载规划
节点。新请求的远场计划4f2aeae922a7827ddaf80c49计算耗时过长，
输出时年龄128.70秒，运行器按120秒有效期拒绝；300秒窗口耗尽。
本轮未发出运动。日志存于 `.ros_log/grasp_attempt_20260908_branch_optimized/`。

## 同一请求内复用冻结观察位

稳定候选循环反复生成和校核相同参考相机位、目标几何对应的观察
视图。增加只在单次_recheck_and_score_stable中存在的view_cache，
复用观察端点及CAD结果；每个候选仍重新建立自己的接近/抓取/抬升
序列与阶段参数。证据深拷贝，跨prepared请求不复用；取消检查保留。

已录制场景12个候选共444个通过的观察视图，基准14.849秒、缓存
3.516秒；所有四阶段矩阵、观察视图、侧面证据和拒绝记录SHA256
一致：c3bf6e3dd0c371dbd2301bcda4aee475dfdec038b268e32aaadc789f18944c78。
318项相关回归通过；另补充不同接触倾角、跨请求隔离后定向3项
通过。diff检查通过。该加速尚需实机验证。

## 配准初值被点密度扰动

90bb576实机远场计划604436b4b61f65b7c94a2277年龄55.20秒，成功
进入阶段10并复用20.86cm观察位。近场请求37于03:56:06输出，
距阶段开始约36秒，本地准备9.40秒；但配准PCA初值12.34度越界，
发布的预览缺少有效3D配准，任务03:56:30超时，无闭爪抬升。
审计保存在根目录 `.ros_log/grasp_attempt_20260908_view_cached/near_audit.json`。

PCA会随可见点密度变化旋转，即使物体不动。新增同一测量点集缺角
及0.2mm噪声两项回归，修复前均YAW_BOUND_EXCEEDED。现在比较
恒等与PCA初值的唯一匹配残差，未匹配/重复点按对应距离上限惩罚；
用拟合更好的初值，随后保留全部原有迭代及最终旋转/位移/证据门。
初值选择不考虑是否容易通过边界，真正10.5度和26mm移动仍拒绝。
multiview_surface共93项通过。

当前只读重采样475/479点保存在 `.ros_log/grasp_registration_profile_20260908/`。
该新样本修复前后均411 inliers、85.80%重叠、0.724mm RMSE、
0.129mm最大修正；不将新样本当作阶段10失败的精确重放。

补充streaming与task序列498项通过（需source ROS及devel环境）；diff检查通过。

## 近场快照等待旧阶段导致过期

ccb9abb部署后，计划552efd97e7ae808604c881d5于04:06:24进入任务，
阶段11于04:06:27开始，近场请求45排在旧远场清理之后，准备12.506秒、
结果年龄20.394秒，触发15秒结果门。该轮未进入配准候选接纳，04:07:27
超时，无闭爪抬升；记录在 `.ros_log/grasp_attempt_20260908_registration_seed/`。

将直接近场的唯一快照采集延后到实际worker空闲。提交时同锁再次
检查worker，若忙则保留采样额度和上次提交时间，不把新快照排到旧
处理之后。远场滚动替换行为不变；阶段60秒和结果15秒上限不变。
新增忙时不采集、不消耗额度、提交竞态两项回归，修复前均失败。

流式回归303项通过，diff检查通过。

## 提前拒绝无法改善的夹指伸达

d178f0e运行计划796e0146ecc1f582cdb06f83进入阶段12，旧请求31于
04:14:04退出，近场34随后才采集提交。准备仍达15.968秒，结果年龄
19.532秒过期；该轮未闭爪抬升，记录 `.ros_log/grasp_attempt_20260908_idle_capture/`。

近场插入倾角围绕同一jaw轴变化，proposal接触中心与jaw线不变。
在倾角二分及完整CAD物化前，使用同一OBB线段和opening-fit余量
证明必然单侧超25mm的方向，记录proposal_reach_rejections并跳过。
保留最终完整门；边界数值留给最终门裁决，远场行为不变。
增加证明超宽方向不进入倾角计算的回归（先失败），加上streaming
和multiview共397项通过。各准备步骤耗时现在进入pipeline metrics，
包括被结果有效期拒绝的请求，便于区分参数、几何配准、候选和输入准备。

同一已录制415/408点样本，使用ccb9abb的相同注册结果，原4个proposal
生成24个候选，全部按单侧伸达拒绝；现在4方向均提前拒绝，保留的
有效候选集合相同（均为空）。物化调用232次变0；cProfile候选生成
4.253秒与0.194秒（主机存在并发负载，耗时仅供诊断）。这不是抓取成功证据。

## 任务端桌面连续性使用目标局部距离

34f7df8实机远场b8ecc9a8806a7fec99318712进入阶段13，近场
4a1c73ba22eed0e506869a09已通过配准（440 inliers、86.96%重叠、
0.841mm RMSE、最大点修正0.112mm、0.0473度）及四段严格MoveIt。
准备分解：参数0.290秒、几何/配准6.665秒、候选3.812秒、输入0.277秒。
任务端仍按原点平面d之差判定5.110mm超4mm，04:25:50超时，没有运动。
两份实际审计已保存 `.ros_log/grasp_attempt_20260908_early_reach/`。

修复grasp_task_node._support_plane_delta：保留单位法向校核和角度限制，
在两份富计划的共享OBB中心锚点比较平面有符号距离。同一实际计划
的d差5.110383mm对应目标局部平面差0.175475mm。与注册器使用同一
物理量，因消息不携带全点云而采用OBB中心（注册器采用点云中位数）。
新增远处目标/法向轻微变化回归，同时确认局部4.01mm仍拒绝；原有
float32精确边界夹具改为在目标锚点转动法向，保持原边界意图。
任务序列198项通过，diff检查通过。需重载任务节点后重新生成计划；
本次部署不需要重启机械臂驱动。
