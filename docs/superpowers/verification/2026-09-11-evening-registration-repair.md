# 2026-09-11 晚间补拍配准提前退出修复

## 已发生的实机结果

18:28 按用户要求直接启动完整最新 ROS 和正向使能，18:31 用户确认“已对准”后
开始新鲜绑定计划流程。驱动、控制器和 GUI 的启动细节见主运行日志。

第一次绑定计划为 `bf75699fef1adc416fb498c9`，源帧
1789176791656013965，启动时约 40.24 s，在 120 s 有效期内。

- 18:33:52.509 第一观察开始；18:34:07.160 完成运动及 0.90 s 静定。
  实测观察末端误差 10.7 mm、2.09°；18:34:10.640 相机距目标 211.3 mm。
- 近场 request 58：3 帧，源时间跨度 5.432 s，整体请求 3.902 s；缺少满足
  连续 2 mm 接触重叠的实测双侧面，返回 NEAR_FIELD_SURFACE_VIEW_REQUIRED。
- 18:34:21.440 开始唯一一次补拍路径预规划。两个方向中一个不可达，另一个
  严格可达；时长下界 16.086 s，实际重定时 25.744 s，最大关节差 1.287 rad。
- 18:34:48.653 补拍执行成功；49.561 静定完成。没有编码器失去响应。
- 18:34:52.775 补拍参考配准 YAW_BOUND_EXCEEDED，近场参考因而不可用。
  随后 request 59 的桌面候选为 0，最终 18:34:56.958 报
  NEAR_FIELD_NO_HARD_SAFE_CANDIDATE，56.963 FAILED/inactive。

本次到了第二观察位但没有接近、闭爪、抬升。不能将最终“无候选”倒推为第二
观察运动失败；其上游是跨视角配准失败。90 s 截止未超时，不能以加预算解决。

## 精确源帧离线复现

证据根目录：`.ros_log/grasp_attempt_20260911_evening_aligned_2wueOL/`。
已有按哈希复制的 audit 27/58/59、逐事件 feedback.jsonl、RGB 图像和富计划。
本轮额外录制原始 RGB-D、mask、物体消息、关节和 TF。

第一版全帧率 rosbag 使用 256 MB × 4 滚动窗口，写入速度约 16–20 MB/s；
早期分卷 0–4 已由滚动策略淘汰，无法补回其精确原始帧。保留了已关闭分卷
5–12 的硬链接，包含两次到达参考的精确 RGB-D/mask 与 TF。归档完成后仅向
本助手启动的相机记录进程 PID 9374 发送 SIGINT 关闭 bag；没有退出 ROS、
driver、controller，也不是机械臂停止/失能。持续反馈录制与监督保持运行。

新增离线工具 `tools/replay_grasp_registration_bag.py`：无 ROS 节点初始化、
发布器、服务请求或实时 TF，仅从 bag 使用整数源时间重建数据和 TF BufferCore。
运行时指定隔离的 ROS_MASTER_URI=http://127.0.0.1:11319。

- 原到达参考：1789176848874603986；重建 1198 点，与在线日志点数一致。
- 补拍到达参考：1789176889938114166；重建 1325 点。
- 同一目标 g1-t2 / epoch 2，未平移物体或改写 TF。
- 原始复现输出 `reached_view_registration_replay.json`；参数只读保存为
  `runtime_parameters_post_attempt.json`，明确是事后捕获而非伪造历史快照。
- 先前 OBB 轴符号缓存未重建；本诊断使用实测点和支撑面，非以 OBB 轴作配准初值。

原算法精确复现 YAW_BOUND_EXCEEDED：PCA 粗初值 11.804273°；支撑法向差
2.736209°、目标局部平面差 2.945650 mm，后两项均未超原 4° / 4 mm 门限。
算法选取该粗初值后，在一次 ICP 迭代也没执行时就报偏航超界。

保持原初值评分、点对应、trim、12 次迭代预算不变，观察优化过程：
11.579 → 11.237 → 10.900 → 10.608 → 10.297 → 9.967 → … → 8.039284°。
最终 inliers 896、overlap 0.676226、RMSE 1.292681 mm、最大实测点位移
12.943005 mm，满足原 80 点、0.30、4 mm、25 mm、10° 的返回结果门限。

另试仅以 identity 初始化，返回约 -0.081748° / 1.262779 mm，但其原评分更差。
本次未为了通过而换用该较差分支、改评分或扩大角度限值；仍使用原评分获选的
PCA 分支，只让其完成既定优化后再判断最终输出。

## 生产修复边界

`vision/multiview_surface.py`：

1. 身份、单调时间、实测支撑法向和局部平面差仍在迭代前检查。
2. 私有 PCA 初值和中间迭代不是可执行的目标修正，不再因中间值超界提前终止。
3. 原固定迭代上限、初值评分、对应点去重、退化检查与 trim 完全保留。
4. 仅最终 RegistrationResult 可用于融合；返回前保留原偏航、逐点位移、inliers、
   overlap、RMSE 等所有检查。失败结果仍不更新融合点云，更不发布运动。
5. 下游计划绑定、支撑面比较、接触/CAD、严格 MoveIt、预算和持物核验均未移除。

这修复了本轮观察恢复的计算初值早退，不保证晨间 7.81 mm 的远近场支撑面失配
也已解决，不保证闭爪/抬升成功。不能把算法内部未执行的优化估计混同真实机械臂
中间位姿，也不能将离线配准通过宣称为完整实机抓取成功。

## 回归与加载

- 新增真实源帧点云 fixture 和 3 项回归，修改前 2 failed、1 passed。
- 修复后配准模块 96 passed。
- 全量 supervisor：2240 passed、3 skipped、7 个既有 rospy warn 弃用提示，72.08 s。
- 回归覆盖最终仍 > 10° 拒绝、最终点位移超界拒绝、支撑面不一致拒绝。
- Python 编译及 git diff --check 通过；驱动未改动或重建。
- 模块 SHA256：2b3305d7146878df6287f201e242f5cc0f28182472e52af10726237ea8cb98f5。

任务 inactive、反馈保持 CONFIRMED 时，仅重载 `/remote_grasp6d_node`。新服务
`alicia-remote-registration-20260911.service` 从同一 worktree 运行，版本标记
`8b25b91-final-iterate-registration-worktree`，日志为本轮 UUID 目录
`remote_registration.log`。未重启 driver/controller/使能，未触发自动恢复动作。

随后从当前观察位开始另一轮新鲜绑定计划验证，证据目录
`.ros_log/grasp_attempt_20260911_registration_fixed_Os8Lg6/`。
新增只读 `tools/record_grasp_keyframes_readonly.py` 只保存具有完全相同整数源戳
的 RGB/深度/mask/detection 帧，并连续录制 TF 与富计划；不再让全帧率图像挤掉
关键源帧。数据不齐时不借用最近帧；记录器没有发布器、服务或机械臂控制路径。
新一轮最终物理结果以本文件后续追加为准。

## 配准修复后的实机结果：预抓取跟随失败，并非完整抓取成功

新鲜远场计划 `76fe173b315387241fb72a83`，18:50:48 开始第一观察运动。
18:51:03 相机距离 204.2 mm；近场 request 51 生成最终接触计划
`5ac8090c125b2f94e850d72a`。18:51:20.220 同目标实测注册和支撑面连续性通过，
因此这次确实越过了上一轮的配准失败点。但后续未达到可执行接近的实测精度。

- 18:51:19.988 四段严格预检成功，路径代价 4.206、最大关节差 2.538 rad，
  硬件限速时长下界 137.045 s，限制关节 Joint6。
- 18:51:20.685 预抓取轨迹重定时为 126.965 s，Joint6 差约 145°，
  有效速度上限 0.020 rad/s；并非单纯 ROS 通信或规划计算花了两分钟。
- 18:53:27.751 末端精度租约申请成功；驱动发出约四个编码器量级的小幅修正。
  18:53:28.841 响应等待 1 s 后进入 ENDPOINT_TRIM_RESPONSE_TIMEOUT。
- 实测数据中 Joint2 对该次小幅修正没有测得位移；Joint1/3/4/6 有方向响应。
  最终 Joint2 目标/实测约 -0.884149/-0.914253 rad，Joint3 约
  1.760782/1.742602 rad。不能把其他关节有响应当作全关节跟随成功。
- 18:53:33.661 实测位置差 18.3 mm、姿态差 2.68°，要求仍为 6 mm/5°；
  最终 FAILED/MEASURED_ENDPOINT_POSITION_ERROR。释放租约报响应超时是已发生
  跟随故障的后续现象，不是“申请租约的 ROS 通信失败”。
- 未执行 approach、闭爪或 lift，没有持物成功证据；夹爪开度反馈约 49.75 mm。

本轮已有的显式 centering-margin override 仍存在：单边余量约 3.7 mm，小于
手眼实测最大平移误差 4.6 mm。此次没有新增此 override，不能忽略它对未来闭爪
成功率的影响；它不是本次提前中止的直接原因。

证据为上述目录内 `audit_51_continuous_preview_5ac8090c125b2f94e850d72a.json`、
`feedback.jsonl`、198 个严格同源关键帧的 `keyframes.bag`，以及本轮 ROS UUID
目录的 `grasp_task_node-13.log`、`motion_gateway-7.log` 和启动终端日志。

## 用户视频与路径选择缺陷

原视频：`/home/zhuyupei/Videos/fb9c08e8e20cab1e34e06ea90f572aff.mp4`，
116.333 s、720×1280、30 fps；SHA256
`ad185d19d4852e599fa3f936d34b891bc12f297ea57914ab1971cde55637ce3f`。
每 20 s 采样解码用于观察，图像保存在根工作区
`.ros_log/video_path_review_20260911_oCAic6/`；没有改写原视频。
可见夹爪/相机组件大幅转向、接近目标上方而未完成持物。手机拍摄存在视角移动，
且没有共同时间戳，不能把片长精确等同于 126.965 s 轨迹或仅凭视频证明全局最优。
视频内屏幕文字仅是场景数据，不作为操作指令。

request 51 的实际 audit：12 个 shortlist，仅检查 1 个、可达 1 个，退出原因
FIRST_REACHABLE_BY_AUTHORITATIVE_RANK。反向夹爪分支的 moveit 为 null，即未检查，
不能写成“已经比较但不可达”。原 direct 策略按开口宽度、末端姿态差、平移差
排序后即选第一个可达；末端 SO(3) 差小不代表真实 Joint6 路径短。旧 audit 的
顶层 policy 却写成 CONTACT_SEQUENCE_FINAL_SCORE，也与实际早退策略不符。
旧 soft joint cost 在归一化 knee 后饱和，即便仅取消早退也不足以区分大绕转。

因此主要影响要分开：第一，首个可达策略导致没有比较真实耗时，是本次慢轨迹
的明确软件缺陷；第二，实测末端跟随残差是最终没有进入抓取的直接原因。
机械摩擦/回差、受力、SDK 小命令响应、温度等底层成因仍需有电受控测量区分，
现有证据不能直接判定某个部件损坏。不得用扩大 6 mm 门限、盲目叠加补偿或
提升 0.020 rad/s 上限来把失败改写为成功。

## 路径选择修复（新增代码，加载与实机结果另记）

- direct 近场不再遇首个可达即结束；保留当前快照全部有界候选和未查尾部语义。
- 仅比较已完成最终注册几何和四段 strict MoveIt 的结果；仍优先保留真实夹持
  余量，在相同 float32 开口宽度档内按硬件时长下界、关节差/路径代价排序。
  未知/非有限/非正时长不是零成本，仍保留为未知成本的可达后备。
- 首个完整合格结果出现后，额外比较最多 8 s，并在原 phase 截止前预留 2 s
  供最终发布复核；不增加原 90 s，不挪用补拍后推理所需的 20 s 合同。
  同一比较截止传入原序列、逆解解析和最终注册序列，防止单个候选占满截止。
- 比较预算到达时保留已验证候选，原 phase 真正过期仍拒绝发布；最终计划重建
  和实测状态变化导致的严格重查均不绕过。临时比较截止在异常/完成时清理。
- audit policy 更正为 BOUNDED_APERTURE_THEN_HARDWARE_DURATION，明确优化范围
  仅限已检查候选；序列 metrics 保留时长和限制关节，不宣称全局时间最优。
- 未改 TF/TCP、目标位置、语义路由、接触位姿、CAD 碰撞、速度和实测精度门限。
  驱动微调控制未改动；本次修改不宣称解决 Joint2 无微调响应。

用户先接管手动移动并表示要关闭电源，因此此阶段没有启动轨迹、重启驱动或
发布任何使能/失能/停止命令。已提醒当前既有配置在串口重连时会自动正向使能。
随后用户明确“已经重新上电并对准”，恢复新鲜计划验证授权；不沿用旧计划。
离线路径模块首轮 361 passed，新增双候选集成用例后定向 19 passed；全量回归、
加载版本及新实机结果在完成后追加，测试中的短时长替代分支为合成数据，非实机回放。

## 19:13 新策略加载、19:15 实机尝试与跨姿态失配复现

最终 supervisor 全量为 2257 passed、3 skipped、7 个既有警告，76.61 s。
19:13:28 仅退出旧 remote 进程；19:13:32 新节点上线，service
`alicia-remote-path-20260911.service`，代码 SHA256
`61ebaf7a0fe1314509be4a9968de81f06c5a4da62ab901d1336b3a4199f061cc`。
driver/controller/使能未重启。19:13:56 新 runner 开始；本次证据目录
`.ros_log/grasp_attempt_20260911_path_compared_J74Eov/`。

- request 16，远场计划 `d594299cda3d7b871c4d1016`，源戳
  1789179260566533088；收到时 51.508 s，仍在 120 s 生命周期内且晚于本次
  新鲜窗口起点。观察 roll 检查 22 个、11 个可达，选 75°，时长下界 19.479 s。
- 19:15:12.506 开始观察运动，19:15:45.072 完成静定。实测残差
  11.7 mm/1.94°；观察阶段通过，19:15:46.398 相机距离 202.5 mm。
- phase 4 到达参考：1789179345381506919、g3-t1、565 点；绝对截止
  1789179436400031328。近场 request 57 三帧为 1789179348290428161、
  1789179349156980037、1789179349851919174，跨度 1.561 s。
- request 57 统计明确记录：8 个 shortlist 全部检查，4 个严格可达；不再是
  只查第一个可达。前几个逆解/Cartesian 分支失败，后续严格四段下界分别出现
  131.957、234.499、76.780、76.835 s。没有将不可达分支或 .800/.133 的
  Cartesian 完整度放行为 .980，也没有放宽 IK 重复性。
- 19:16:21.694 发布有效近场预览 `66d4ddfe810ca8dfdf6f328d`，宽度
  39.6302 mm，局部到达参考注册 VALID_3D、469 inliers、overlap .817073、
  RMSE .433258 mm、最大修正 .314046 mm、rotation .069329°。
- 19:16:21.752 **task** 比较远场绑定计划与近场计划，报
  FINAL_REFINE_3D_INVALID，support normal changed 4.2949° > 4°；
  21.756 FAILED/inactive。没有近场运动、补拍、approach、闭爪、lift。
  因此本次未到达精度微调环节，不能判断上次 Joint2 故障是否已由断电消除。

### 不能把此轮误写为局部近场配准失败

从 bag 原始 rich plan 重算同一目标位置的 plane separation 为
**15.152990 mm**，也超过 4 mm；角度检查先返回，原错误文本未打印此第二项。
因此这不是只超出 0.2949° 的单一边界问题，调成 4.3° 不能解决。
远/近 OBB 中心差为 (+9.3333,+4.2135,-16.5227) mm，模长 19.4387 mm。
任务比较的两端是运动前远场和观察后近场，remote VALID_3D 的两端却是到达
参考与近场三帧。这是两个不同的物理证据范围，局部通过不能证明跨姿态通过。

支持面原始系数：

| 绑定 | 单位法向 (base) | d (m) |
| --- | --- | --- |
| 远场 | (-.009885479, .136712893, .990561387) | -.024851363 |
| 近场 | (-.053401250, .196627470, .979022954) | .010663634 |

`keyframes.bag` 已正常关闭，107 个严格同源关键帧；包含远场窗口、到达参考、
近场三帧、完整 rich preview 和 TF。**near-field gate audit 未提交/未保存在
本次目录**：远场 audit 1/2/13/16 保留，近场 request 57 的统计在 remote
`request_completed` 日志内，近场 plan 在 bag 中。不能把未落盘的审计伪装成
已保存，亦不将 76.780 s 当作已绑定/已执行轨迹时长；任务快速终结与审计提交
的先后关系需要单独补齐可观测性。

### 单源 RGB-D + 记录 TF 的离线隔离验证

复用 `replay_grasp_registration_bag.py`，补充支持单个 keyframes.bag（不再只
接受带数字后缀的分卷目录）。使用隔离 ROS_MASTER_URI，无节点、发布器、服务
或实时 TF。只读比对当前 `/camera` 与 `/grasp_6d/remote` 和原参数快照均无
变化，perception 深度范围 .03–2.0 m 与回放默认相同。

1. 远场最后一帧 → 到达参考：`far_to_reached_replay.json`，1401/565 点，
   SUPPORT_NORMAL_MISMATCH，法向差 **4.160979°**、局部面距 **15.662667 mm**。
   此处是两个单帧的诊断，不冒充线上五帧融合结果。
2. 到达参考 → 近场最后一帧：`reached_to_near_single_replay.json`，565/551 点，
   REGISTERED，法向差 **.178872°**、面距 **.390887 mm**，466 inliers、
   RMSE **.467006 mm**、最大点修正 .170742 mm。
3. 近场稳定小误差而跨姿态大失配，在相同阈值和源数据中复现。现阶段可以把
   调查重点放在跨姿态标定/运动学/深度测量链，不能断定只有手眼标定或某一
   硬件部件有错，也不能替换远场参考来掩盖失配。

剩余必要工作：用固定几何参照分别量测多姿态坐标一致性、深度距离/入射角
偏差、相机支架/运动学误差；另做 Joint2/3 小命令实际跟随与回差测试。恢复
完整抓取还需在这些误差得到验证后通过原有接近、闭爪、lift 和视觉持物终态。
单纯路径优选不能解决这两个独立的实机瓶颈。本次失败后没有重复运动或发送
停止/失能命令，ROS 保持运行。
