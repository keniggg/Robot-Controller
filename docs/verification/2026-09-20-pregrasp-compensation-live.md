# 2026.9.20 新版节点加载与首次补偿实机抓取

后续用户要求修复本次问题，[部分响应补偿修复](2026-09-20-pregrasp-adaptive-response.md)记录代码与验证进展。本页保留首次实机原始结果。

用户明确要求重新加载节点并自行运行 6D 抓取。本次已完成加载和一次新鲜视觉计划的真实执行，**最终失败，未接近、未闭爪、未提起**。预抓取补偿真实运行了一步，反馈 FK 位置残差从 **18.330024 mm 降至 16.547458 mm**，未通过原 6 mm 门。停止原因是本步 Joint5 `+4 count` 指令得到 `0 count` 实测响应。

## 版本与加载核验

- 代码：`c7abe056d476ccab16b70e481b17c0c89cc40fc3`，分支 `codex/pregrasp-compensation-20260920`，实际目录 `.worktrees/protocol-v3-upgrade`。
- 原任务空闲、GUI 手动直控关闭、驱动为 `CONFIRMED:MEASURED_DIRECTIONAL_RESPONSE`，新鲜反馈和纸盒检测正常；起始温度报告 33–40°C。
- 通过已有 systemd 服务重启 task：PID `61743 → 159839`；通过原 roslaunch 的 respawn 重载 gateway：PID `45505 → 160019`。
- 新 `/supervisor/compensate_pregrasp` 服务类型匹配。仅新增运行参数 `/grasp/pregrasp_cartesian_compensation_enabled=true`，已采集的其他参数逐项比较无变化，重载前后六轴实测位置一致。
- driver PID 仍为 `7197`。相机、GUI、remote、控制器及驱动未重启；无零点/标定修改。
- [部署与最终结果](evidence/2026-09-20/pregrasp_compensation_live_result.json)包含源文件哈希、PID、参数比较、动作和状态记录。

## 实际执行

时间为主机本地 PDT，日期 2026-09-20；原始记录保留 ROS 秒/纳秒时间戳。

1. 00:28:22 开始新鲜视觉窗口，使用已有 `FreshPreviewRunner`，不复用昨日计划。
2. 远场计划 `0bcdf5e03dd3cd9cac05edf3` 完成稳定性、几何和严格规划检查；精确核验 plan_id 后调用一次 `/grasp/start`。
3. 00:30:11–00:30:28 执行观察运动，控制器成功，实际反馈稳定。观察阶段 FK 残差 11.313247 mm / 1.894°，通过该阶段原有相机距离和实测 CAD 门；这不是接触阶段 6 mm 到位验收。
4. 近场计划 `39cd0f09257e7c1039fff1eb` 通过检查。00:31:06–00:32:36 执行约 90.628 s 的预抓取轨迹，控制器成功，随后实测稳定。
5. 00:32:38–00:32:41 执行第一条 3 s 补偿轨迹，实际最终路径验证通过，显式逐轴路径容差 0.06 rad，控制器成功。
6. 00:32:42 反馈判定 `PREGRASP_NO_BOUNDED_DIRECTIONAL_RESPONSE`，任务转为 `FAILED / inactive`；推理停止。未继续第二步补偿，无自动回退或失能。

## 第一步命令与反馈

原视觉目标保持为 `[-0.1150937773, -0.4209914612, 0.1439575422] m`；姿态 `xyzw=[0.2426220911, 0.9482222875, -0.1205856638, 0.1657350660]`。

| 关节 | SDK 参考前 → 后 | 实测前 → 后 | 命令增量 | 实测增量 |
| --- | --- | --- | --- | --- |
| Joint1 | 748 → 746 | 752 → 750 | −2 | −2 |
| Joint2 | 1544 → 1544 | 1527 → 1527 | 0 | 0 |
| Joint3 | 3067 → 3071 | 3052 → 3055 | +4 | +3 |
| Joint4 | 1804 → 1800 | 1811 → 1807 | −4 | −4 |
| Joint5 | 1137 → 1141 | 1140 → 1140 | +4 | 0 |
| Joint6 | 3042 → 3046 | 3037 → 3041 | +4 | +4 |

位置残差 `18.330024 → 16.547458 mm`；姿态残差 `2.793041 → 2.359676°`。这些值已用运行时 URDF 和实测计数独立重算，与在线日志一致。模型 SHA256 为 `890592a24b8ad818d995e69beb670ea4254bf84f589615bfac3c8562f62449b7`。仍是编码器 FK 残差，不是外部绝对测量。

补偿控制器完成后约 3.70 s 的 **38 帧 accepted SDK 反馈**均为 `[750,1527,3055,1807,1140,3041]`；约 47 s 后只读快照仍相同。该窗口未发现延迟响应。此处是 accepted 反馈，**本轮没有另行保存原始串口字节，不等同于此前 75 帧原始回包证据**。夹爪整个记录区间保持 49.75 mm，未出现接近/闭合/提起阶段。

Joint2 本轮被策略保持，不能据其零变化认定再次拒绝新指令。Joint5 原实际位置已比 SDK 参考高 3 count，目标从 1137 增至 1141 后仅高于实际 1 count；因此不能把本步现象直接解释为 Joint5 损坏或完全不能运动。它说明“其余五轴都按命令增量响应”的离线假设在本次姿态不成立。是否存在死区、静摩擦或目标接收问题，仍未区分。

## 证据与当前状态

- 本地原始目录：`/home/zhuyupei/alicia_wa_full/.ros_log/grasp_pregrasp_compensation_20260920_002450/`。
- `keyframes.bag` 已正常关闭，含 204 组同源 RGB-D/检测关键帧、3392 条 accepted 反馈、4984 条 SDK 目标、4 对轨迹 goal/result、任务状态和完整 ROS 日志；约 234 MB，保留本机。
- `before_reload.json`、`after_reload.json`、`observation_reached.json`、`final.json` 保存阶段状态；图片保留本机。`keyframes_audits/` 保留精确哈希绑定的规划审计。
- [结果 JSON](evidence/2026-09-20/pregrasp_compensation_live_result.json)、[本机文件校验清单](evidence/2026-09-20/pregrasp_compensation_live_manifest.json)。

执行中出现驱动 E1/E2 状态提示，日志按已有策略处理；没有持续测温超限触发的失能记录。保留该现象，不据此归因为过热，也不将状态码提示当作新的温度实测。

新 task/gateway 保持运行，补偿参数已启用。任务已释放执行槽、保持自动模式，机械臂停在本次补偿后的姿态。本轮没有在失败后复位补偿额度、放宽响应/到位门或追加运动。下一轮算法需要依据逐轴实测响应处理不响应轴，而不能继续采用其余五轴均响应的假设；进一步实机动作仍须通过新的完整计划与路径检查。
