# 2026-09-19 第二轴小步响应与后续抓取实测

## 当前结论

尚未成功抓取。对准姿态下 Joint2 的单轴 +7 编码单位命令产生了 +5 单位实测响应；同日近场预抓取姿态下，包含 Joint2 +7 的多轴到位校正中，Joint2 实测响应为零。不能把前一个姿态的响应当作所有姿态、方向和负载下的验证，也不能据此断定机械损坏。

本次代码与运行节点位于 `.worktrees/protocol-v3-upgrade`，不是 IDE 显示的根工作区旧驱动。本轮没有发送 torque_off、归零或停止使能命令，没有放宽 6 mm 接触前到位要求。

## 本轮修复与验证

1. 网关新增独立、显式启用的 `characterize_joint_response` 诊断入口。最多 7 个编码单位，仍受原局部 0.012 rad 连续路径、支撑/目标几何、控制权、实测反馈及场景有效期约束。每个控制参考 epoch 在实际提交前锁定一次；重载网关不能重新获得已消耗的执行机会。
2. 有界探测轨迹改用冻结的成功发送 SDK 参考作为名义起点，另外核对实测状态与该参考的原 0.035 rad 合同。修复了从落后的编码器起点规划而人为产生回退的问题。第一次诊断在最终连续路径证明处被拒，SDK 和编码器增量均为零；没有把未提交轨迹算成物理试验。
3. 驱动支持逐轴最大校正步长，空数组保持原标量规则。运行参数仅把 Joint2 的上限设为 7，其余为 4：`[4,7,4,4,4,4]`。此值尚未写为 launch 默认值。无响应仍进入故障保持，不允许继续叠加校正。
4. 对精确编码格边界采用与响应判据一致的浮点舍入余量，覆盖“命令 7、响应 5、剩余 2”的数值边界；没有增加物理容差。

离线验证：相关规划/参考交接测试 155 项通过；驱动 C++ 测试 98 项通过；驱动协议/诊断相关 Python 测试 63 项通过。驱动已重新编译、加载，加载源码与二进制哈希保存于下述实机目录。测试通过不等于第二轴跟随问题已经修复。

## 单轴诊断结果

证据：根工作区 `.ros_log/joint_response_20260919/`。

第二次诊断 `result2.json`，场景计划 `cd35bc430c0624d198df0cd1`：

| 项目 | Joint1–Joint6 编码单位增量 |
| --- | --- |
| 成功发送的 SDK 目标 | `[0,7,0,0,0,0]` |
| 实测编码器 | `[0,5,0,0,0,0]` |

只证明当时对准构型的这次单轴响应；没有标定绝对位置，也没有完成抓取。

## 驱动加载后的两次抓取

首次记录：`.ros_log/grasp_joint2_20260919/`。计划 `2822b508af7e2607fa44bab0` 完成了有界使能响应前缀；主观测轨迹在最终跟随支撑包络证明处被拒，报 `following support box unresolved at precision limit`。主观测轨迹未提交，没有闭爪。

第二次记录：`.ros_log/grasp_joint2_20260919_retry/`，闭合关键帧 bag 完整保存。

- 远场计划 `cafc3f3cffa4b99d66de1246` 实际完成观测位移动；主观测轨迹重定时 12.912 s，最大单关节变化约 0.646 rad。到位实测 CAD 最小支撑净空为 60.227 mm。
- 近场计划 `c38490fea67a4327e25c6136` 完成预抓取运动。43.303 s 只是初次重定时时长；控制参考桥段的连续速度证明再次拉长了轨迹。bag 中实际 action goal 时长为 **61.368589 s**，并非规划额外花了 18 秒。已经修复服务结果只报告初次时长的问题，追加实际提交时长与参考桥段缩放倍数；这是报告修复，不改执行速度或轨迹。
- 预抓取到位校正于 ROS 时间 `1789808984.311091` 开始，1 秒后响应超时。实际 SDK 增量与编码器增量如下；不能用日志四舍五入后的角度代替实际发送计数。

| 项目 | Joint1 | Joint2 | Joint3 | Joint4 | Joint5 | Joint6 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| SDK 增量 | 0 | +7 | +3 | +2 | 0 | -4 |
| 实测增量 | 0 | **0** | +4 | +3 | 0 | -4 |

Joint2 的 SDK 目标从 1617 变为 1624，实测保持 1600。超时后的 78 个原始关节回包与 accepted 编码器值一致，约 7.7 秒内没有迟到响应。其他参与校正的轴有响应。

最终末端位置残差 **16.959 mm**，姿态误差约 **2.40°**；z 比请求值低 **16.419 mm**。控制器参考 FK 与请求位置只差 **0.0897 mm**。按固定模型分解，Joint2 的误差对 z 的贡献约 -11.651 mm，Joint3 约 -4.690 mm。这是同模型关节跟随位移分解，不是外部标定真值或硬件故障鉴定。

任务在接触前失败，未进入接近、闭爪、抬升。第二轴的逐轴步长调整没有通过此预抓取构型的实机验证。尚不支持继续增大补偿或放宽到位要求来宣称成功。

## 后续只读诊断及控制权

通过现有驱动的固定只读 `query_motion_diagnostics` 查询取得真实回包：十个原始速度字均为 0，自检掩码 `0x03FF`、异常通道位为 0；同窗口温度 36–44℃。文件为 `device_status_after.json`。这些是较晚的静止窗口，不能倒推失败瞬间内部控制器状态，也不证明小步响应正常。此前运动中存在 E1/E2 状态事件，仍保留原始证据，未据此猜测固件寄存器或修改保护设置。

ROS 时间 `1789809158.722662090`，GUI 切入手动控制，SDK 和实测姿态随后改变。因此后续姿态不是原失败点的继续保持证据。`1789809301.949633791` GUI 已释放手动模式；用户随后明确回复“已重新对准并交权，双手已离开，可以继续”。已据此启动下一轮，未再次索取相同授权。

## 重新对准后的第三轮：交接竞态和观测采样空档

记录：`.ros_log/grasp_joint2_20260919_realign3/`。执行时长记录修复通过 104 项测试后重载网关；驱动、控制器、GUI 保持运行。新鲜窗口 `1789809906855103254`，选中远场计划 `79601a7d4bc15cea24b976f1`，但在实际观测运动提交前失败。

`handoff_race.json` 记录了交接竞态：同步 hold 发布后，控制器仍短暂发布**新时间戳、旧静止参考**；旧逻辑只等待旧时间戳或插值中的参考，因此提前报 `controller has not reached the selected reference`。随后控制器正常到达新参考，驱动也发布 `sdk_tracking`，但任务早已失败。本轮没有关节抓取运动，不能把控制器内部交接插值当成实体执行。

修复限定在 `frozen_sdk_handoff`：只有驱动仍为 `sdk_handoff_required`、SDK 字保持不变、控制器位置精确等于本次原参考且速度/加速度为零时，才继续等待**原有截止时刻**。目标已匹配不能误入等待；不同参考、提前解除驱动门、控制权/epoch/反馈异常仍拒绝。没有重发、重置超时、使用编码器重基准或提前授权轨迹。相关测试最终 **146 passed**；新增覆盖延迟正常完成、永不消费命令、不同静止参考与驱动门提前解除。

同轮选中了 roll180°，但未执行。冻结场景显示 20° 倾转未通过原 0.12 rad 跟随盒净空证明；30° 倾转不满足实测侧面信息量要求，因此退回大滚转。这是离散观测候选的空档。

在同一冻结场景中，25° 候选的相邻 IK 分支最大单关节变化为 0.795982 rad，名义支撑净空 75.4 mm，候选整条路径通过原 0.12 rad 加半编码格的跟随支撑盒证明。另保留了 28° 的离线比较，但只向生产候选新增 25°，原 10/20/30° 不删。角度仍不超过 30°，严格检查最多 6 次和启动新检查的 3 秒预算不变。候选列表容量由 3 增至 4，属于采样数量调整，不改物理门限。相关远端/观测测试 **573 passed**，新增冻结场景回归。

## 第四轮：25° 观测通过，近场规划失败

记录：`.ros_log/grasp_joint2_20260919_tilt25/`。加载交接修复和 25° 候选后，沿用用户最新交权，从新鲜窗口 `1789810825455221652` 开始。

远场计划 `beb1a0f0be8a77985be556ed` 选择 roll0°/tilt25°，最大关节变化 0.798 rad。实际提交轨迹 **15.969 s**，实测到达观测位后 CAD 支撑最小净空 **68.347 mm**。本轮已通过参考交接，没有执行大翻腕。ROS 时间 `1789810943.640922` 进入近场规划；此处只记录阶段结果，尚未闭爪或证明第二轴校正恢复。

随后近场在 90 秒期限内未找到完整路径，报 `NEAR_FIELD_DIRECT_TIMEOUT`。最后分支日志中的 Cartesian fraction 0–0.011 不是全部分支的上界；完整 rosout 中 136 次结果的最高完成比例为 0.8253012，仍低于原要求 0.98。未提交预抓取动作，也未闭爪。`cartesian_fractions.json` 保存统计。为能复现未提交审计的过期近场请求，新增只读 solver 输入日志，包含四阶段精确位姿和服务实际起始关节；相关测试 77 项通过。

## 第五轮：候选比较使源图像过期

记录：`.ros_log/grasp_joint2_20260919_near_retry/`。任务执行器等待 300 秒后退出，没有调用新的抓取。计划 `6f22e447cfe0a9eca4d93289` 已完成候选审计，但整个 70 个观测候选的比较到 ROS 时间 `1789811623.389910` 才结束；选中者早在 `1789811424.059430` 获得严格规划结果，最大关节变化仅 0.105 rad。最终发布时原图像已过期，因此不能执行。

修复：首个经过完整路径证明的候选出现后，仅再允许 8 秒内启动比较；进行中的检查保留原截止条件。仍按已经验证的候选轨迹时长排序，剩余候选明确记录为未检查，不声称全局最优。不延长图像有效期，不改运动或净空门限。相关测试 **574 passed**。

随后记录到 GUI 于 `1789811684.138506` 再次切入手动，于 `1789811780.163034` 释放。用户再次确认“已调整完成并交权，双手已离开，可以继续”；下一轮沿用此最新授权。

净空证明还加入一个有界的提前反例搜索：仅在根关节盒的 Taylor 下界不确定时，按最差 CAD 角点梯度检查一个盒内角点；计入原计算预算。只有精确 FK 的实际模型净空违反原门限才提前拒绝，角点通过仍须完成原区间证明。相关 118 项用例中首跑 117 通过、1 项旧固定反例数值断言失败；改为独立 CAD/FK 复算与盒内包含验证后，该文件 22 项全部通过。没有修改生产几何门限。

## 暂停后恢复最新完整 ROS，等待重新对准

第六轮目录 `.ros_log/grasp_joint2_20260919_bounded_comparison/` 的运行器只进入候选计算，后报 `unable to contact master`，没有记录新抓取提交。用户随后要求先暂停，再要求恢复完整 ROS 并等待重新对准。恢复时系统无 ROS master、无旧 Alicia 用户服务；未重放旧计划。

使用当前 worktree 的最新 `full_system.launch` 启动服务 `alicia-ros-resume-latest-20260919.service`。实际展开的 launch 与日志保存在根目录 `.ros_log/ros_resume_20260919_latest/`：机械臂、MoveIt、网关、GUI、相机、双触觉、识别、远端推理客户端、任务、夹爪与监测节点均启动。沿原配置启动即正向使能，串口 `/dev/alicia_arm`、1000000 baud，自检轮询 0；保留此次逐轴校正上限 `[4,7,4,4,4,4]`。未发布失能、归零、抓取或额外诊断运动。

ROS 时间 `1789867876.610770` 驱动请求正向 `torque_on`；操作者随后手动移动，`1789867894.516581` 获得 `CONFIRMED:MEASURED_DIRECTIONAL_RESPONSE`。目标检测有效，任务 `IDLE/active=false`，GUI 手动控制中。建立限时 1800 秒的纯订阅服务 `alicia-resume-watch-20260919.service`，记录 watch.log；不将被动日志等同无限在线主动监督。

启动后的原 WSL `/health` 地址 `http://172.23.132.97:8000` 连接被拒绝；已请求用户给出当前服务地址或启动该推理端。当前不发送图像、不启动抓取，等待本次对准和推理服务恢复。

用户回复“已经重新启动wsl端”后，同地址 `/health` 已恢复：`ok=true`、protocol 3、GraspNet 模型 `loaded=true`，CUDA 与 MuJoCo 服务正常返回。ROS 端保持原地址，未重启驱动或发动作。当前等待用户这次“已对准”后再开始新鲜计划与自动抓取。

## 最新对准后的尝试与泛化要求纠正

用户“已对准”并在中断后要求继续。确认 GUI 已释放、任务 IDLE、正向使能实测确认后，从新源窗口 `1789869197678995132` 发起一轮。记录为 `.ros_log/grasp_resume_aligned_20260919/`；任务始终 IDLE，闭合 bag 中没有 FollowJointTrajectory goal、controller command 或 joint_commands，只有 SDK 保持流。未执行新的观测、闭爪或抬升。

请求 23 的冻结 SDK 起点名义 CAD 净空 56.367 mm，但 ±(0.12+π/4096) rad 六轴盒中存在 -6.288 mm 模型反例。较晚请求 58 为名义 56.655 mm、反例 -5.769 mm。反例不是实物碰桌或当前位置不可达的证据。此时换观测终点无法改变相同起点盒，因此不应继续重复检查 24 个候选。

新增软件修复（尚待此轮重载）：本地跟随包络拒绝不再继承 `STRICT_SERVICE_SUCCESS`，流水线保留具体的 observation 拒绝码；同一远场请求的共享起点拒绝后结束搜索，未检查尾部保持未分类，后续请求重新计算而不缓存失败。相关 609 项首跑 608 通过、1 项旧接口断言失败；更新远场 continuation 的断言后对应 2 项通过。原失败报告与最终结果分别保留。

曾离线计算“保持姿态抬高 2 cm 后起点盒可通过”，文件 `start_height_offline.json` 仅是模型 IK 与静态误差盒，不是完整路径或执行许可。据此要求用户手动抬高的建议已被用户明确否定，**现已撤回**。不得再以用户移动机械臂或目标来满足此算法前置条件，也不得把同一姿态补丁冒充整个工作空间的泛化修复。

用户最新要求：覆盖机械臂整个可抓取工作空间的通用路线，从不同起始姿态、目标位置与朝向自动寻找过渡、观测和完整抓取路径。需区分位置 IK 可达、完整任务可行与有限搜索尚未找到解，不能把候选搜索失败写成物理不可抓取。

正在核对规划误差范围与实际控制约束的绑定。已读本机 Noetic 控制器源码：per-goal path_tolerance 可覆盖默认路径容差，但 `PATH_TOLERANCE_VIOLATED` 分支只将 action 标记 aborted、清空活动 goal，仍调用硬件命令 update；事后 cancel 已终止 goal 也不会进入 hold。因此不能只把规划盒改小或修改 action 容差就声称真实跟随/停止已受保证。此处记录发现，尚未修改控制器、驱动或提交新轨迹。

### 复核文件

- `endpoint_count_analysis.json`：校正前后成功发送目标、实测计数及原始回包保持窗口。
- `endpoint_parameters_after.json`：失败时任务误差和后来采样的实时关节值，各自时间不同，不能当作同步状态。
- `following_attribution.json`：固定 FK 模型位移分解、运动中跟随误差与原始状态直方图；轨迹末段与校正后保持窗口分别标记。
- `action_timing.json`：实际 action goal 时长、容差字段和控制器结果。
- `device_status_after.json`：固定只读查询发送证据和设备实际回包。
- `runner.log`、`watch.log`、`keyframes.bag` 与 `keyframes_audits/`：计划、执行、逐轴反馈及冻结场景。

## 通用观测执行约束修复（2026-09-19，待实机验证）

新增 `observation_tracking_contract.py`，所有起点/目标使用相同有限搜索：[0.09, 0.06, 0.05, 0.035] rad 每轴路径容差，上限不超过控制器已有配置。原全局 0.12 rad 校验函数保留，新分支只有执行器能明确下发同一 per-goal path_tolerance 时启用。误差盒另加 SDK 半计数与 Noetic 指令停止包络：以严格检查的 0.08 rad/s、0.30 rad/s² 和 0.5 s 停止时长计算 0.03025 rad 预留；连续曲线、起点、终点及 controller bridge 均检查。3 mm 净空、夹爪几何、目标 OBB、关节界限与端点容差未降低。

新增 `contracted_trajectory.py`，向同一 `/alicia_controller/follow_joint_trajectory` 提交最终轨迹和显式容差。提交前重验场景、控制权、URDF、夹爪、配置、轨迹哈希和静止参考；失败不允许端点近似判定翻为成功或回退 `go()`。Noetic action aborted/超时后请求空轨迹 hold；手动接管或外部 preempt 不覆盖新操作者。hold 保持关节使能，不发送 torque_off。此设计仍是条件几何验证，不证明硬件超调、通信延迟、标定精度或实物停止距离。

离线结果保存在根 `.ros_log/grasp_workspace_generalization_20260919/`：原有集成流程 551 项通过；执行约束和停止/手动优先 69 项通过；追加各执行入口与最终提交变更拒绝测试 44 项通过（与前组有重叠，不求和）。5 个不同底座方位样例使用同一规则通过。当前场景两点假设路径在 0.06 rad 路径约束下通过条件 CAD；该路径不是实际 MoveIt 轨迹，不构成执行授权或整个工作空间覆盖证明。原第一次样例包含超出 URDF 的关节位置并被正确拒绝，后改为有效采样方位；保留原报告。

这是解除固定误差盒导致所有候选共享起点失败的一项通用修复，不代表已解决加载后的 Joint2 无响应、完整抓取成功率或全工作空间搜索覆盖。将用新图像、新计划自动验证；用户手动抬高建议继续撤回。

### 加载后的第一轮与队列时效根因

已仅重载 motion_gateway 与 remote_grasp6d，驱动 epoch 与关节使能保持不变，配置启用执行绑定。根目录 `.ros_log/grasp_contract_live_20260919/` 保存 source hashes、watch、runner 和关闭后的 keyframes.bag。新源窗口 1789871684536304950，运行器 300 秒未获得有效预览退出；没有实际轨迹或闭爪，新执行约束尚未经过实机轨迹验证。

多个请求在 WSL 返回前后的 ROS 调度阶段超过 15 s 结果时效；数学库为系统参考 BLAS，不能把它误诊成 OpenBLAS 线程过多。发现 `complete()` 在本地候选处理之前将 pending 提升成 next，worker 却要等本地处理完成才准备这个已保留的旧源。期间更新的图像进入另一个 pending，下一轮仍先计算旧源；导致额外过期与稳定性等待。新实现 `claim_for_preparation()` 在真正启动计算时，用同代、同目标且源时间更新的 pending 替换尚未启动的保留请求，保留原始 source stamp，记录一次 PENDING_REPLACED，不替换已经准备中的请求。取消/代际/目标切换继续走原拒绝逻辑。此修复不改变 15 s 时效或 3 次稳定命中要求。

### 第二轮实机：大翻转已发生，取消偏晚，未抓取成功

`.ros_log/grasp_contract_latest_20260919/` 新源 1789872440972174406；队列修复后请求 1/6 进入稳定性，结果年龄 12.7/14.2 s。请求 11 生成预览 `2a2598abfba1c9eaa37d72d2`，源时间 1789872469285997390。审计确认 30°/25° 小幅候选的 strict IK 已成功，但本次新增导数约束以 `command derivatives exceed stopping-envelope contract` 将其拒绝，未先做时间伸缩。后续 roll=180° 被选中，Joint6 计划变化 2.985 rad，轨迹 59.698 s。

这是本次新实现遗漏时间参数处理导致的反效果，不能称为修复成功，也不能归因于用户摆放。轨迹于 1789872596.259 提交。发现后向精确 action goal `/motion_gateway-1-1789872596.259` 发 cancel，实际取消约 1789872654；已发生约 2.887 rad（165°）运动，取消偏晚。控制器 PREEMPTED，任务 FAILED，没有闭爪或提起。整个过程未发送 torque_off，网关原失败保持还重新请求正向使能。

新增修复：同一静止参考下对完整连续曲线测量速度/加速度界限；超过界限时按 max(v/vmax,sqrt(a/amax)) 统一延长时间，同时缩放速度和加速度，不改变任何关节位置或路径几何，再完整验证并绑定新哈希。preview 和实际提交统一通过 `_prepare_controller_reference_timing`，不能只在执行前补时间、却在候选筛选阶段误删短路径。测量与失败信息保留具体导数数值。当前尚待此次测试及重载。

### 导数时间修复完成与新手动接管

143 项控制参考、执行入口、导数/停止模型测试通过；已重载网关和 remote，将统一时间伸缩用于预览及执行。`.ros_log/grasp_contract_latest_20260919/action_contract_actual.json` 确认上一实际 action 的 6 轴 path_tolerance 均为 0.06 rad，结果 PREEMPTED；未把其 error_code=0 误当成功。

第三轮目录 `.ros_log/grasp_contract_retimed_20260919/`，新源 1789872902389049291。纯订阅监控记录 1789872883.389556 GUI_DIRECT=True，关节随后被用户手动移动。发现后暂停运行器与推理；此轮没有自动轨迹，保持关节使能，等待这次调整后的明确交权。运行器末尾 service no response 是暂停中的取消，不代表抓取成功或驱动断连。不得沿用手动操作前的确认提交自动动作。

离线覆盖工具 `tools/audit_observation_workspace_support_readonly.py` 使用固定记录支撑平面和 4×3×3 共 36 个底座/肩/肘构型样例；其中 12 个超出模型关节限制被拒绝，24 个 FK 构型有效。14 个原盒可通过，6 个在执行绑定约束下新增条件通过，3 个名义夹爪几何与支撑冲突，1 个仍未通过条件盒。报告 `.ros_log/grasp_workspace_generalization_20260919/workspace_static_samples.json`。样例包含不同高度、距离和方位，**仅为静态夹爪/支撑覆盖，不证明整臂无碰、连续路径、全工作空间搜索完备或实物抓取成功**。保留越界和未通过样例，不用当前姿态通过代替全空间结论。

## 用户再次对准后的实际验证（本地 2026-09-19 20:xx）

用户明确要求继续处理、完成后自行抓取，并说明机械臂已经对准。沿此授权将 GUI 从手动交回自动，保持最后 SDK 目标及关节使能，未重启驱动。根目录 `.ros_log/grasp_aligned_final_timing_20260919/` 保存此轮 source hashes、runner、watch 与 keyframes。实时订阅工具新增 action goal/result 的 ID、持续时间、逐轴目标及路径容差；纯订阅，不具备动作权限。

新源窗口 1789873435188790798。请求 15 选择 roll=0°、tilt=30°，max_joint_delta=0.820 rad，Joint6 约 0.108 rad；预览 ID `6afd4975e6a994dd04cc00a9`，源时间 1789873464305042743。实际 goal `/motion_gateway-1-1789873539.740`，时长 16.400663374 s，六轴 path_tolerance 均为 0.09 rad。1789873556.194 返回 SUCCEEDED/error_code=0。观测到位后 0.90 s 收敛，实测夹爪 CAD 支撑净空 75.543 mm，阶段测量通过。此次没有重现上一轮 171° 腕部翻转。

这是第一阶段实际通过，不能写成完整抓取成功。1789873559.989 任务进入等待近场预览；1789873563.474 捕获达到视点后的实测表面，源 1789873557348408937、1092 点。继续等待完整接近、抓取和提起序列规划及执行反馈。

## 第一阶段成功后的补拍失败与通用方位搜索

上述运行最终在 1789873579.224 失败，尚未闭爪/提起。近场请求 51 的多视图配准有效：865 内点、0.781 重叠、0.601 mm RMSE；返回 NEAR_FIELD_SURFACE_VIEW_REQUIRED 要求补看另一夹持面。原补拍仅生成两个固定方位、固定滚转的相机姿态，二者严格 IK/规划失败，不应归结为目标超出整个工作空间，也不应要求用户抬高机械臂。

保持到位后的实测关节、冻结目标中心/OBB/支撑面以及实际相机安装关系，先扩大光轴滚转（20 个姿态）仍无逆解；再在相反夹持面半球上搜索方位 -75/-45/-15/15/45/75 度与 8 个滚转角，共 48 个候选，6 个在实时碰撞场景中有 IK。最小最大关节变化约 0.454 rad。只读完整规划进一步验证其中两个零滚转分支：实际时间参数化后为 9.732650239 s 和 10.321886717 s，均通过 0.09 rad 执行路径跟随约束及含指令停止预留的连续夹爪支撑检查。证据保存根目录 `.ros_log/grasp_aligned_final_timing_20260919/clear_view_grid_paths_readonly.json`；它只是候选证明，不是已执行或硬件跟踪/停止认证。

任务节点新增物体坐标系方位搜索与保持目标光线的相机滚转搜索，默认先覆盖相反半球两端，再扩大滚转；不使用当前目标的硬编码空间位移。搜索检查完整定时轨迹，按实际时长/关节路程排序；找到合格候选后最多继续比较 8 s，整体搜索 25 s（在候选之间检查），并保留近场运动、稳定和重新推理预算。未搜索候选单独记数，不能写成不可达。更早的获胜候选必须重新规划并重新验证，最后执行仍经过 gateway 当前场景/参考/模型校验。物理补拍次数仍为一次，原净空和接触精度不变。

只读 ROS 状态/规划请求曾遇到自动审批超时，未执行；允许的规划重试成功，未因此改动使能。此处完成的是补拍搜索修复及两个候选的只读路径验证，后续实机结果继续追加。

补拍回归：255 个不同用例通过（当时 unittest fixture 被收集两次，报告为 482 passed），修正重复收集后路径选择专项 5 passed。另补冻结服务 Header 与 measured TF Header 不同的回归，确保按实际发送的请求验证目标 hash，输入测量不被改写。新功能仅用于 far-field 观测规划器，接触阶段原有复拍规划接口保留。

## 扩展补拍搜索的实机结果（持续记录）

仅重载空闲 grasp_task_node，运行 `alicia-clear-view-task-20260919.service`；驱动、控制器和使能保持。最后针对相关路径选择/补拍调用检查 20 passed。新一轮目录位于根目录 `.ros_log/grasp_clear_view_grid_20260919/`，源窗口 1789875054910546541。远场 plan `8ff2d1a5e07cc79232b9e7bf` 的原始源 1789875090350438117；任务直接保留已经到达的相机视点，无重复第一阶段运动。

近场请求 105 仍缺另一侧表面，触发扩展补拍搜索。候选 1 实际 goal `/motion_gateway-2-1789875188.784`，轨迹 9.724622758 s、最大关节变化 0.450 rad、六轴 path tolerance 0.09 rad；1789875198.536187 返回 SUCCEEDED/error_code=0。随后稳定 0.90 s，实测 CAD 支撑净空 75.698 mm。补拍前两固定方位无解的问题在这轮实机上解除。

补拍后请求 106 成功生成近场 plan `56ffe494e18741626de2da6d`，源 1789875206769033670。融合 4 次观测，1611 点，配准 848 内点、0.756 重叠、0.992 mm RMSE；最终完整 pregrasp/approach/grasp/lift 序列通过严格 MoveIt 检查。1789875243.408354 已提交预抓取动作 `/move_group-1-1789875243.407541669`，时长 101.395 s，Joint6 需约 1.431 rad 旋转以形成夹持朝向。此记录时尚未闭爪、未提起，继续监督实际末端反馈，不能记为抓取成功。

### 本轮最终失败点：预抓取末端第二轴跟随

101.395 s 的预抓取动作于 1789875344.859634 返回 SUCCEEDED/error_code=0。随后精度租约微调发生 ENDPOINT_TRIM_RESPONSE_TIMEOUT；1789875350.875 任务在进入接触前失败，位置残差 15.744665 mm、姿态 2.277°，z 残差 -15.472648 mm。未执行接近、闭爪或提起。使能保持，未发 torque_off。

`endpoint_count_analysis.json` 对照关闭后的 bag 与晚些时候 8 秒只读采样：原控制器参考 FK 距请求位置仅 0.073603 mm。Joint2 SDK 成功发送目标在第二次微调由 1526 到 1533（+7 编码单位）；整个微调窗口从 1525 到 1533（包括此前 1 单位参考变化，共 +8）。Joint2 实测始终为 1506。其他参与校正的关节有实际响应。超时后 75 个原始关节回包、accepted JointState 一致，持续 7.4 秒；1789875516.616 至 1789875524.819 的后续采样仍完全保持相同 SDK 目标及实测计数，没有迟到响应，也没有操作者换姿态。

同一模型的六轴 Shapley FK 分解显示 Joint2 对 z 误差贡献 -12.797 mm，Joint3 -2.733 mm。此处明确区分同模型跟随误差与外部绝对标定；没有证据足以判定机械损坏。速度/自检只读查询只取得“已入队”服务返回，未捕获设备回应；源码在目标仍被视为活动时延后查询，故不能据此宣称设备自检正常。未重复放大校正、清除响应故障或放宽 6 mm 要求。

已向用户询问当前第二轴/连杆是否有外部接触、线缆拉扯或新增负载，仅请求工作区外观察，无需移动机械臂。这是定位实际响应失败所需的现场信息，不是再次索取相同的抓取交权。后续接触抓取受未解决的第二轴负载响应阻塞；补拍规划及实机补拍已通过不等同整个抓取任务成功。

用户随后将现场观察答复更正为“未见接触或拉扯，末端配置未变”。以前一个相反回答推测现场受阻的前提已撤销，未据此修改控制。用户现场观察不能进一步分离固件接收、摩擦/回差与内部伺服处理。

新纯订阅 `alicia-endpoint-hold-watch-20260919` 启动时，1789875869.943594 收到 GUI_DIRECT=True；随后记录实际关节和 SDK 目标改变，1789875898.838707 GUI_DIRECT=False。当前新构型约 [-1.8285051, 0.4586603, -0.2040194, -0.0475534, -0.2162913, 0.1365243]，与失败点不同，不能再作为原失败点的迟到响应窗口。已请求确认此次调整完成、交回控制和双手离开；在收到前仅做只读分析，不提交动作。

### 本次重新交权后的反向响应验证

用户已再次明确确认调整完成、交权且双手离开。为区分方向相关小步不响应，诊断工具新增 `--delta-counts`，仅允许 -7 或 +7，默认 +7；网关既有七计数、连续路径、参考状态、控制权和每 epoch 一次的约束未改。相关单元测试 17 passed。

根目录 `.ros_log/joint2_reverse_response_20260919/`：新场景 `e06a7a2a75005de7e435db41`，参考 epoch 1789875872459441743。3.000 s 动作 `/move_group-2-1789876414.332706265` 于 1789876417.384 返回 SUCCEEDED。SDK 只对 Joint2 从 2352 减至 2345（-7），实测仅 Joint2 变化 -6，其余五轴为零。结果证明该重新对准构型下反向小步可以响应，否定“一切反向小步都无响应”；不能证明先前预抓取负载构型的 +7 校正恢复。诊断未闭爪，也未启动新的完整抓取，没有回退或额外叠加。

当前仍未成功抓取。需先得到预抓取构型的可靠响应/校正依据；未用新构型的单次响应替代验证。已整理 `2026-09-19-joint2-response-open-questions.md`，具体列出缺少的目标接收、插补、死区与正式 Follower 调参信息，未向外部发送。

## 2026.9.20 归档与后续授权

用户要求先将最新完整代码上传 GitHub 并标记 2026.9.20，再修改第二阶段预抓取的软件补偿。验收仍为视觉自动定位与抓取，不能固定摆位或示教。[修改前基线与归档 JSON](2026-09-20-pregrasp-baseline.md) 固化上述实测边界；后续开发结果单独记录，不回写为本轮抓取成功。

后续开发追踪：[2026.9.20 预抓取补偿](2026-09-20-pregrasp-compensation.md)。记录中的初始状态加假设响应可达 5.66 mm；这是离线结果，本页原失败实验结论不变。
