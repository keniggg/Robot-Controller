# 2026-09-19 观测翻腕与近场取景修复

运行代码位于 `.worktrees/protocol-v3-upgrade`。本次在已有未提交修改上增量修复；未改动驱动或失能接口。

## 失败证据

原始记录：主工作区 `.ros_log/grasp_aligned_20260919/`。
远场计划 `f7f97efca109c762a1527f2b`，源帧 `1789801771758391618`。
执行观测时最大关节变化约 3.058 rad，Joint6 接近 175°，重定时时长 61.167 s。
这只是受限搜索中通过的分支，不是已证明的全局最优。

补充倾转候选只有 5/10/15°，按标称距离先检查。第一次近桌面候选的完整跟随包络证明就耗尽了 3 s 的“开始新检查”预算；8 个倾转候选未检查。保留原滚转回退后，最终选到了 180° 相机滚转。

近场请求 116、117 都没有可执行接触候选。融合数据分别有 1288、1857 个实测点。约 55 × 33 × 20 mm 目标的宽边超出夹爪可用行程，窄边缺少有效双侧接触高度。原补拍横移 40 mm 后仍在窄边同一侧。

请求 117 的配准对实测点最大位移为 10.9915 mm。融合点云已映射到阶段参考坐标，早期候选的 OBB、中心与姿态仍来自当前快照，原计算直接混用了两者。

## 代码变更

- 将有界倾转采样改为 10/20/30°。严格检查前，按标称支撑净空从大到小安排倾转候选，覆盖全部原距离；原滚转回退顺序保持。最终仍按通过完整证明的路径时长排序。
- 审计明确记录只比较已检查且通过的候选，记录是否完整检查；不再把预算内结果描述为全局最短路径。
- 当前快照的候选生成、实测夹持宽度、接触高度和碰撞几何统一使用逆配准后的点云。原始融合点云、来源帧和点来源索引保持不可变。
- 配准后的最终接触计划使用参考点云，同时对接触中心与目标 OBB 施加同一次配准；物理支撑净空继续使用当前冻结支撑平面。陈旧、失败或异目标配准不能用来转换候选。
- 仅“双侧表面不足”触发的恢复动作改为向可夹窄边的另一侧提出两个候选。保持相机手眼变换、观测距离、原单次补拍约束、严格 MoveIt 规划和全部原有净空约束；普通遮挡补拍逻辑保持。

## 离线验证与限制

同一冻结几何中，30° 倾转、22 cm 距离存在最大单关节变化约 0.8005 rad（45.9°）的解。以 0.05 rad/s 构造的 24.0145 s 路径通过原连续净空与跟随误差包络证明。

另外调用正在运行的 MoveIt **仅规划/逆运动学**服务，从历史起点得到同一姿态的邻近关节分支，并生成 20 点整臂规划。该规划再次通过连续支撑包络证明。没有执行这些历史数据路径，24 s 不是实机测量时长。

对面视角仍可能不可达：原场景中保持当前高度和 21.5 cm 距离的对面视角在离线 IK 中碰到 Joint5 下限；更靠近且更接近俯视的解又被原补拍非接触包络拒绝。本次没有放宽这些限制，不能据此宣称实物抓取成功。

永久证据归档：主工作区 `.ros_log/grasp_repaired_20260919/offline_evidence/`。
实际新一轮执行结果与最终测试结果另记于同目录的会话记录。

## 单次实机验证

首批变更通过 863 项相关测试后加载。用户重新确认当前位置已对准并交权，另外明确确认相机/点云/场景数据发送至既有 WSL 地址。

计划 `ff5d0aa0900ace86bf9a3980` 选中 0° 滚转、30° 倾转；最大关节变化 0.835 rad（约 48°），轨迹重定时 16.701 s。严格执行成功并稳定，实测最小支撑净空 71.215 mm。第一阶段大翻腕没有重现。

完整抓取仍失败：近场出现可规划接触候选，但配准后的最终抬升 Cartesian fraction 为 0.938，低于原 0.980。后续候选规划耗尽 90 s 时限，未闭合夹爪。记录在 `.ros_log/grasp_repaired_20260919/`，219 帧关键帧 bag 正常关闭。

针对该证据，追加了最终配准序列的自由姿态重求解：只允许既有契约内的预抓/抬升旋转，接触姿态与所有位置保持；对新序列再次做配准坐标一致的 CAD 和完整严格规划，通过后才更新计划哈希。失败、证据不全或超时均不能改写已哈希计划。追加变更通过 583 项相关测试后加载；第二轮所选候选直接通过原严格序列，未触发该回退，不能据此宣称回退本身已实机验收。

## 第二轮实机结果与视频

用户重新对准、交权、确认双手离开，并在登录审批恢复后要求继续。记录位于主工作区 `.ros_log/grasp_repaired_20260919_retry2/`，873 组关键帧 bag 正常关闭；完整细节见该目录 `session_summary.md`。

远场计划 `4ce4fc7dbd54baf0f3c5b5f7` 再次选择 0° 滚转、30° 倾转，最大关节变化 0.831 rad，轨迹重定时 16.613 s。实际观测执行成功，到位 CAD 最小支撑净空 63.174 mm。

近场请求 96 选中计划 `8c20696f3c20231a11a96725`，完整四阶段规划通过，近场预抓取运动也实际执行完成。接触前实测到位检查失败：位置残差 16.920 mm、姿态残差 2.46°，其中实际 tool0 的 z 比目标低 16.621 mm；位置要求仍为 6 mm。未进入下一段接近、闭爪或抬升。

到位微调时，实际发送 SDK 计数增量为 `[-4,+5,+4,-4,0,+4]`，实测响应为 `[-5,0,+5,-5,0,+4]`。Joint2 对本次微调没有可测响应，1 秒响应窗结束后进入 `ENDPOINT_TRIM_RESPONSE_TIMEOUT`。超时后 79 个原始关节回包与 accepted 值一致且不变。控制器参考 FK 与计划预抓取位置仅差 0.043 mm，残差不能用本次 IK 或编码量化解释。尚不能唯一确定固件接收、死区、摩擦、回差或负载等根因；没有增大补偿或放宽接触门限来跳过这一事实。

后续只读采样显示 SDK 目标和实测姿态已经改变，因此该窗口不作为同一失败目标的迟到响应证据。采样时任务 inactive、机械臂 motion_enabled=true。没有发出停止使能命令。

用户提供视频 `/home/zhuyupei/Videos/1b0e9401f21d95ed5005a2fe2f6304cb.mp4`，时长 140.7 s。抽取 0–140 s 每 20 s 的关键帧，能看到夹爪逐渐转正并移向盒子上方；末段保持张开，未见夹持抬起。视频支持第二阶段对准运动有进展，与预抓取实际执行记录相符。由于手持视角变化且缺少外部标定，不能据此量化毫米精度，也不能将画面中的夹爪—物体距离等同于日志的 tool0 到位残差。
