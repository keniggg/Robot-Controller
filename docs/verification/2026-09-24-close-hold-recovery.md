# 2026-09-24：接近遮挡、闭合保持与预抓取补偿

本次实机记录仍为失败，未闭爪。用户随后关闭机械臂电源和各硬件接口，要求离线继续；下列新增补偿没有部署或执行。没有启动硬件、发布运动或去使能命令。

## 已定位的两个独立问题

1. 原接近失败：direct 模式直到 approach 才允许遮挡，导致 pregrasp 中的 `TARGET_LOST` 撤销冻结计划。修复后，在新鲜的执行仿真和检查点通过后，允许未知物体 contact plan 在 pregrasp 开始进入预期遮挡阶段。目标身份改变、确认的位置漂移、碰撞、取消及其他真实故障仍终止执行。
2. 最新记录 `20260924T054836.781510Z_d43b6f78dee04bf390524ce2edb18be7`：遮挡修复实际生效。预抓取轨迹成功，日志出现 `Preserving frozen 6D execution authority through close-range target occlusion`。之后 8 步补偿将 FK 位置残差从 17.554217 mm 降至 6.667775 mm，第 3 关节达到原始 +32 count 总量上限，最终 `PREGRASP_NO_BOUNDED_IMPROVING_STEP`。第 1、6 关节先后因弱响应保持，第 2、5 关节按历史策略从一开始保持。

源数据为本机原始录包中的 SDK、accepted 编码器和补偿日志；[紧凑回归数据](../../src/alicia_flexible_grasp_supervisor/tests/fixtures/pregrasp_following_20260924_actual.json) 保留全部实际步骤、同一模型哈希、固定目标和冻结计划。不能把新命令的假设响应写入这些历史实测行。

## 离线完成的补偿修改

`/grasp/unknown_pregrasp_joint2_response_probe_enabled=true` 只用于未知物体。常规补偿搜索无可改善步骤时，允许对第 2 关节提出一次隔离的小步补偿。候选为原有 ±2/±4 count，只有模型预测能减小固定目标误差且保持姿态、跟随差和关节限位时才提交给原严格路径执行器。

此尝试共享原始 SDK 起点、32 count 总量、12 步和 60 秒预算；提交前仍检查完整路径、控制器接续段、碰撞、停止预留和当前控制权。实际响应必须至少同向 2 count、无过冲或未指令关节异常运动，且实测 Cartesian 残差降低。无响应即失败，不连续累加、回退或重置同一 plan/epoch 预算。第 5 关节仍保持，其他模式不启用此分支。

本次历史末端的剩余整数终点共 561 个。在保留原有保持轴和单位响应假设下，最小残差为 6.666302 mm，仍不能通过 6 mm 门。新分支提出 `[0, 4, 0, 0, 0, 0]` 的第 9 步。**仅在明确的假设响应下**，第 2 关节实际移动 2/4 count 后分别为 5.884779/5.306711 mm；这不是实机到位或抓取成功。

用原冻结场景和历史控制器配置构造的 3 秒合成轨迹，通过了原条件路径检查，含跟随误差盒的支撑面净空下界约 3.026 mm。配置来源和合成轨迹性质均明确标记；这不是实时 MoveIt 路径，也不授予执行权限。下次执行必须重新规划并验证当前路径。

## 闭合后保持

`/grasp/unknown_lift_after_close_enabled=false` 控制真实动作；`/mujoco_digital_twin/unknown_lift_gate_enabled=false` 控制仿真抬升结果是否硬性阻断。当前会话启动文件分别设置了这两个开关，WSL 启动方式不变。仓库默认仍保留原有抬升行为，使用其他启动入口时应显式传入所需策略。

不抬升分支在闭合命令成功后保持原位，不发送抬升、退回或松爪动作。仅有新鲜、有效、无滑移的持续双侧真实触觉证据，才自动报告 `SUCCESS`；缺少证据则为 `HOLDING / GRASP_HOLD_UNVERIFIED`，录制结果为 `unknown`。任务响应现在保留具体失败原因，不再只返回 `failed`。

## 验证与下次加载

最终相关回归一次运行 **422 passed，7 条既有 rospy 弃用警告**，覆盖补偿、网关、任务、控制权、步骤/时间限额、无响应/反向/过冲、模式范围、遮挡及录制结果。`git diff --check` 通过。历史实际失败按原数据保持失败；新增恢复命令的成功测试明确使用假设响应。

下次完整启动应加载本工作树更新后的 `grasp_task_node` 和 `motion_gateway`。会话启动文件为主工作区 `.ros_log/ros_live_20260922_session/resume_modes.launch`，已持久化上述策略；无需改动 WSL。重新上电后必须使用新识别和新计划，历史计划只用于回放。**尚未完成新补偿分支的实机验证，也尚未确认实际抓取成功。**


版本化的[完整会话启动配置](config/2026-09-24-close-hold-session.launch)保存了当前 ROS 节点入口、WSL 两个端口、夹持策略和相机预设；工作树绝对路径已替换为 ROS 包路径。它仅为启动配置备份，本次上传没有执行该文件。运行它会启动真实硬件并按用户当前配置启用关节扭矩。原始视频、录包、构建目录和临时安装令牌保留在本机，不属于源码提交。

上传前补充离线校验：WSL 启动器、接触区间、候选去重及基础远端节点合跑 197 项通过。两个候选选择测试补齐了 ROS 参数读取替身，关闭 ROS 后可独立运行；候选去重测试通过模块引用复用夹具，避免重复收集整组基础测试。生产抓取逻辑未因此改变。
