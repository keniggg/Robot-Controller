# Unknown-object pregrasp visibility repair — 2026-09-23

The operator video `31b3b537316259b4c115ae5d98515135.mp4` (130.17 s) and the recorded generation-9 two-stage attempt show the arm reaching pregrasp without descending or closing. The actual task reported `FINAL_REFINE_REFERENCE_UNAVAILABLE` at Unix 1790146597.360784. This is a failure, not a successful grasp.

Evidence is preserved in `.ros_log/ros_live_20260922_session/contact_repair/` at the main workspace: `gen9_near_audit.json`, `view_contract.json`, `near_view_color.png`, and timestamped operator video frames. The record is `20260923T065202.048826Z_b0f578c19c484bc28505719d3eaf04de`.

The selected pregrasp predicts target image coordinates approximately (343.4, 484.4) in a 640 × 480 image, at optical depth 0.169 m. Thus it satisfies the operator's 0.07–0.50 m depth range but loses the target below the image. The actual camera frame shows only the upper part of the target at the bottom edge. Perception reported insufficient support spatial coverage, then the final post-arrival observation deadline expired. This failure does not establish a new intrinsic or hand-eye calibration error.

The original configuration treats camera visibility as a soft preference. Of 60 evaluated near-field variants, 24 passed the existing visibility calculation; the selected variant did not. This is a concrete selection defect when fresh post-arrival visual refinement is mandatory.

`mode_aware_remote_grasp6d.py` now requires visibility for unknown-object candidates at selection and rechecks the exact final sequence after registration/orientation resolution. Every runtime parameter refresh preserves this requirement, with optical depth constrained to 0.07–0.50 m (or stricter configured limits). Switching back to carton restores its previous policy. Existing approach, grasp, lift, gripper force, collision checks, physical actuation and recording parameters remain unchanged. The repair rejects unsuitable observation poses; it does not claim that every surviving candidate is reachable or passes contact simulation.

Validation: 99 mode, task and runner tests passed. A compact actual-generation-9 fixture reproduces the out-of-frame candidate, accepts an in-frame candidate, rejects a registration correction that moves it outside the image, and verifies carton policy restoration. The repaired remote node was restarted while the task was inactive. There has not yet been a physical grasp with this repair. Fresh operator alignment is pending because the completed attempt left the target outside the usable camera view; old execution plans are not reused.


## Generation 10 turn rejection and bounded planar view correction

Generation 10 (`20260923T071655.126551Z_055f2f6824eb48718889fe6fdda45de9`) selected a visible contact family whose checked maximum joint delta was 3.03543331164935 rad (approximately 174 degrees), limited by Joint6. This violated the requested avoidance of large winding. The agent cancelled only the active FollowJointTrajectory goal `/move_group-2-1790147951.302782630`; it sent no torque-disable command. The controller installed a continuous desired-command hold and requested positive enable. Task terminated with `PREGRASP_COMPENSATION_FAILED` following the cancellation, without gripper closure or lift. The record's failure reason and actual-result evidence explicitly identify that cancellation rather than claiming an object-contact failure.

Unknown-object selection now applies the existing 90-degree observation turn bound to near-field candidates too, including the exact final registration-bound sequence before publication. Carton policy restores its prior values. Small pregrasp pitch alternatives passed projection and endpoint CAD but failed strict MoveIt reachability; they were not deployed. The final implementation instead constructs a bounded view offset parallel to the measured support plane. It shifts only pregrasp position, at most 40 mm and within the existing pregrasp distance bound. All four orientations, approach/grasp/lift poses and contact point remain unchanged. The direction follows the current camera optical-axis intersection with the target-height support plane; no scene-specific coordinates are hardcoded. If none of these proposals meets the unchanged view/range limits, the candidate remains rejected. All existing CAD, IK, full sequence and physics gates continue to apply.

The actual implementation replay on generation-9 geometry found a 25 mm offset (base xyz approximately [0.004686, 0.024282, -0.003664] m). The projected target was (336.75, 422.04) at depth 0.175 m with 54-pixel margins. The strict four-stage MoveIt check returned success, joint_max_delta=1.1672517104427476 rad and path cost=2.122599659329524. This was read-only, retained original geometry timestamps and is not authorization to execute an old plan. Evidence: `contact_repair/view_offset_implementation_replay.json`. Regression validation: 258 tests passed, including actual-view projection, open-gripper CAD clearance, bounded plane-parallel offset, unchanged contact poses, explicit large-turn rejection, and carton restoration.

The user confirmed alignment again through the asynchronous question. Remote node was restarted with the final implementation, and `alicia-recorded-grasp-view-offset-20260923.service` started generation 11: record `20260923T074219.462845Z_8c156e0742004376ad4e4d17aa649c2f`. At 07:43 UTC this new attempt is still planning; physical grasp success has not been established. Recorder remains the original 600-second configuration. The previously verified second-oldest record was reverified and its registered data cleaned after generation 10 completed, preserving the latest five complete local records. Archive worker was resumed; free space was approximately 3.54 GiB before generation 11.


## Generation 11 actual result and generation 12 direct attempt

Generation 11 reached the far-field observation pose, with fresh camera distance 0.2548 m. It failed before contact motion at `FINAL_REFINE_3D_INVALID`: target-local support separation 0.005607307 m exceeded the unchanged 0.004 m bound. This is not a raw plane-offset comparison defect: `_support_plane_delta` already evaluates both planes at the same local anchor. Exact bag previews show center delta [0.002032272, -0.000270661, -0.005713257] m and support-normal change 2.426 degrees. Near-view registration alone was internally consistent (177 inliers, 86.34% overlap, 0.3533 mm RMSE), so it does not independently validate the cross-view base-frame transform. No plane threshold, calibration transform, or task authority was loosened. Evidence: session `contact_repair/gen11_support_comparison.json`. Actual task result and reason are recorded as failure. The original recorder's existing task-inactive completion path closed this record at 07:44:08 UTC; its 600 s maximum was not altered.

The target remained freshly locked near pixel (313, 231), at approximately 0.2548 m, with motion_enabled=True. Under the operator's existing alignment and strategy authorization, generation 12 used the existing direct strategy from that current view. Record: `20260923T075223.511876Z_4162d3da5073462eb94c88273cf5e080`, started 07:52:23 UTC. Four candidates reached genuine MuJoCo: two lacked simultaneous bilateral closure, and two lost contact during lift (actual object rises approximately 0.55 mm, below the unchanged 15 mm requirement). Two final candidate wrist turns of 3.118/3.128 rad were rejected by the new 90-degree bound. Planning terminated with `NEAR_FIELD_DIRECT_TIMEOUT`, without `/grasp/start` or physical contact/closing/lift. Recorder followed its original duration path and closed at 08:02:37 UTC. Manifest result is failure, derived from the explicit planning terminal. Evidence: session `contact_repair/gen12_direct_audit.json` and the independent original bag.

Additional historical-only diagnostics tested NoSlip iterations 10 and 50, without changing mass, friction, closure force or success thresholds. Both still failed retained-contact lift (about 2 mm rise). A separate static-hand diagnostic preserved original finger CAD, inertias, actuator settings and the scene while removing arm dynamics; contact still flickered during the stationary hold. These experiments do not establish a passing grasp or a validated replacement model and are not deployed. Source timestamps remain historical. [MuJoCo's contact-model documentation](https://mujoco.readthedocs.io/en/3.2.0/modeling.html) was consulted for the numerical slip experiment; it does not establish that the real object will slip.

After generation 12, free disk space is about 1.59 GiB, below the configured 2 GiB recording reserve. No currently eligible record may be deleted: the latest five are protected and the two older local records still need successful uploads. The repaired independent archive worker prioritizes completion order and releases the queue after each record so verified older data can be reclaimed before later large uploads. Its 95 archive/transport regression tests passed. No new recording may begin until capacity recovers; camera, driver and joint enable remain running. Physical grasp success remains unresolved.
